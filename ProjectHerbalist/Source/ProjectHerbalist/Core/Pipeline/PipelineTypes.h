// PipelineTypes.h
#pragma once

#include "CoreMinimal.h"
#include "Core/Types/HerbalistCoreTypes.h"

// ============================================================================
// Структуры для внутреннего использования в пайплайне
// ============================================================================

/**
 * Результат агрегации ингредиентов.
 * Содержит нормализованное направление, отдельную магнитуду и мета-параметры.
 */
struct FAggregatedState
{
    FL2Direction Dir;       // Направление (нормализовано на сфере)
    float Magnitude = 0.0f; // Сила эффекта
    FMeta Meta;             // Мета-параметры (агрегированы отдельно)

    FAggregatedState()
        : Magnitude(0.0f)
    {
    }
};

/**
 * Дельта-изменение.
 * Используется для передачи разницы между агрегированным состоянием
 * и текущим состоянием биома перед применением операторов Morok/Zaryana.
 */
struct FDeltaState
{
    FL2Direction Dir;
    float Magnitude = 0.0f;
    FMeta Meta;

    FDeltaState()
        : Magnitude(0.0f)
    {
    }
};
