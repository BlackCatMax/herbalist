// HerbalistSensation.h
//
// Строка ощущения при осмотре предмета (DESIGN_Diegetic_Interface.md,
// решение пользователя 2026-09-21: «осмотр в руке + строка ощущения»). Одна
// фраза вместо таблицы чисел: чем тянет, чем пахнет, что не так. Собирается
// по словарю из воспринятого состояния -- так же, как фольклорное имя зелья
// собирается в HerbalistNameUtils.
//
// Чисел в строке нет никогда (07_UX §7.1). Строка читается из того же
// состояния, что видит травник: при высоком Мороке она ошибается так же, как
// ошибается вид предмета, -- это не баг, а весь смысл восприятия.
#pragma once

#include "CoreMinimal.h"
#include "Core/Types/HerbalistCoreTypes.h"

namespace HerbalistSensation
{
    // «Тёплый, живой в пальцах; с душком, бьёт в нос.» Первая часть --
    // куда тянет направление (или «ровный», если никуда особенно), вторая --
    // до двух самых заметных свойств меты. Если заметного нет -- только
    // первая часть.
    PROJECTHERBALIST_API FString Describe(const FRealState& Perceived);
}
