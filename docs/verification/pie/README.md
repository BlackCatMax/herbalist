# Проверка в PIE по разделам

Ручная проверка игры в редакторе, по системам. Каждый раздел — короткий:
зачем, команды и клавиши, какая строка лога подтверждает. Порядок уровней
проверки (сборка → данные → headless → PIE → долгий прогон) и чтение логов —
`../ENGINE_VERIFICATION_GUIDE.md`; здесь — только уровень PIE. Что поставить
на карту и назначить в редакторе до проверки —
`herbalist_docs/Herbalist_Vault/03_Technical/Current/Level_Assembly.md`;
как устроена каждая система — технический документ её главы GDD
(`03_Technical/Current/NN_<глава>_Tech`, например `07_UX_Tech`).

| Раздел | Что проверяет |
|---|---|
| [0. Проход стабилизации](00_Stabilization_Pass.md) | временный: один проход по всему, что сделано и не проверено в игре (2026-09-22); удаляется после прохода |
| [1. Запуск и паспорт мира](01_Start.md) | собрался ли мир: разметка, регионы, посев мест, ввод |
| [2. Сбор, инвентарь, отрастание](02_Harvest_Inventory.md) | сбор растений и воды, инвентарь, инструмент, намерение, контейнер, возврат растений |
| [3. Варка, зелья, станции](03_Alchemy_Stations.md) | стол, применение к клетке, сушка, отстой, выпаривание |
| [4. Состояние мира и отладка](04_World_State.md) | снимки порчи, клетка, наложения, граф биомов, стриминг, трасса тиков |
| [5. Места и сущности](05_Places_Entities.md) | разговоры, проявления, курганы, капища, точки интереса |
| [6. Община, сад, обереги, хозяйство](06_Community_Homestead.md) | Молва, торговля, грядки, посадка, обереги, база, хранилища |
| [7. Заряна, Травник, артефакты, Буян](07_Zaryana_Journey.md) | фрагменты, Роса, Травник, дары, артефакты, перья, исходы |
| [8. Сохранения](08_Saves.md) | как сохранять и загружать, что сохраняется, сценарии |

## Как читать

- **Команды контроллера** (`HarvestHere`, `SaveGame`, `TalkTo X Y`) вводятся в
  консоль как есть. Строковые аргументы с пробелами или запятыми — в кавычках.
- **Команды менеджера сетки** (`ShowShrines`, `ShowJournal`,
  `ShowZaryanaStatus`, `DumpTrace`, `ReplayLastTick`, `SeedPointsOfInterest`)
  консоль сама не находит: Exec-функции ищутся только у контроллера, пешки,
  HUD и GameMode. Вызывать через `ke * <Команда>` — движок вызовет функцию у
  каждого объекта мира, в ответ напишет `Called '<Команда>' on everything in
  the world and N instances succeeded`.
- **Консольные переменные-команды** (`Herbalist.Graph.Print`,
  `Herbalist.Debug.ToggleCellDistortion`) — как есть.
- `X Y` — координаты клетки: из паспорта мира (раздел 1) или из `IA_Info`.
- Строки лога в таблицах скопированы из кода (`UE_LOG`) или из настоящего
  прогона; `…` — меняющиеся числа и имена.
- Нашли расхождение — порядок действий «Что делать при находке» в
  `ENGINE_VERIFICATION_GUIDE.md`.

## Поддержка

Новая Exec- или консольная команда обязана попасть в свой раздел:
`py tools/docs_audit/audit_docs.py --only C12` покажет неописанные.
