--- a/design.md
+++ b/design.md
@@ -8,7 +8,7 @@

 В системе есть только один нелинейный механизм – **Morok** (в переводе с древнерусского – «морок», «наваждение»).

-Когда вы работаете в грязных условиях, с низкой чистотой или под высоким давлением, Morok искажает свойства зелья:
+Это единственное место, где вносится **динамическая нелинейность** (искажение приращения Δ). Остальные преобразования линейны или используют гладкие монотонные функции контекста, что сохраняет детерминизм и предсказуемость.

 - Оси начинают «стягиваться» к крайним значениям (сильно положительным или отрицательным).
 - Мета-параметры искажаются, делая результат непредсказуемым.

@@ -10,7 +10,7 @@

 ## Основная идея

-Каждое растение (или другой ингредиент) описывается набором «поведенческих тегов»: например, `healing` (целебное), `night` (собрано ночью), `swamp` (болотное), `fire` (огненное). Теги преобразуются в четыре числовые оси:
+Каждое растение (или другой ингредиент) описывается набором «поведенческих тегов»: например, `healing` (целебное), `night` (собрано ночью), `swamp` (болотное), `fire` (огненное). Теги преобразуются в четыре числовые оси:

 - **Тело** (физическое воздействие)
 - **Разум** (ментальное воздействие)
 - **Дух** (магическое воздействие)

@@ -19,9 +19,10 @@

 Каждое зелье также получает пять «мета-параметров»:

+Каждое зелье также получает шесть «мета-параметров»:
 - **Сила** (насколько выражен эффект)
 - **Чистота** (однородность, отсутствие противоречий)
 - **Стабильность** (устойчивость свойств)
 - **Резонанс** (согласованность осей между собой)
 - **Коррупция** (степень порчи, отклонения)
+- **Искажение** (distortion) – уровень непредсказуемости, вычисляемый в процессе варки

@@ -63,6 +63,8 @@

 ### Путь жертвы
 Герой пробуждается и видит перед собой Бабу-Ягу – свою возлюбленную. Оказывается, он спал, а она годами искала способ его пробудить. Всё пережитое – отражение её реальности: она отдала душу и жизнь за него.
+**Путь иллюзии** (открывается при 8-11 фрагментах) – Герой понимает, что мир вокруг – морок, наваждение. Он может принять иллюзию и остаться в ней, либо развеять её, что ведёт к одной из других концовок. Это промежуточный путь, раскрывающий природу Morok.

 ## Главный герой

@@ -75,9 +75,9 @@

 ## Роль Морока и Заряны в цикле

 - **Морок** – искажает итоговый результат после завершения вычислений, создаёт ложные закономерности, влияет на интерпретацию.
-- **Заряна** – повышает читаемость системы, снижает уровень шума, помогает формировать корректные выводы.
+- **Заряна** – её состояние (спит/пробуждена) является основной целью игры и влияет на доступные концовки. Нарративно она помогает герою не терять надежду, но не влияет на числовые параметры.

 Обе силы действуют на протяжении всего цикла.

@@ -155,7 +155,7 @@

 ### Искажение (Morok)

-Искажение применяется к результату **один раз, после завершения всех вычислений**.
+Искажение (Morok) применяется к промежуточному состоянию **один раз** после агрегации ингредиентов, но до этапов разрешения конфликтов, коллапса и структурирования (Zaryana).

 Это означает, что:
 - взаимодействие ингредиентов происходит детерминированно
-- нелинейное искажение добавляется только на финальном этапе
+- нелинейное искажение вносится на определённом этапе, после чего следуют дополнительные преобразования

@@ -204,6 +204,8 @@

 | Мёртвая (из-под кургана) | -Тело, -Разум, -Дух, -Природа, -Сила, -Чистота, +Коррупция |

+Обычная вода считается бесконечным ресурсом и не расходуется при варке. Она всегда доступна игроку, если не выбрана другая вода.
+
 Вода расходуется при варке, собирается из источников или создаётся через обряды.

@@ -233,12 +233,12 @@

 | Способ получения | Условие |
 |:---|:---|
-| **Общение с духами** | Правильное подношение + высокое уважение (respect_score) |
-| **Исследование капищ** | Очистка капища или длительное пребывание в месте силы |
+| **Общение с духами** | Каждое третье уникальное знакомство с сущностью (счётчик encountered_entities) |
+| **Исследование капищ** | Действие `cleanse_shrine` при spirit_pressure > 0.7 |
 | **Создание особых зелий** | Зелье с intent = `ritual` и высокой чистотой (purity > 0.8) |
-| **Сюжетные моменты** | Автоматически при продвижении по основной истории |
-| **Сны** | После определённых действий игрок видит сон — это даёт фрагмент |
+| **Сюжетные моменты** | Достижение флагов `found_wolhv`, `reached_buyan`, `spoke_to_zaryana` |
+| **Сны** | (реализованы через сюжетные флаги, отдельного условия нет) |

@@ -244,7 +244,7 @@

 Каждый найденный фрагмент:

-- **Повышает `clarity`** — игрок начинает видеть более точные описания свойств ресурсов
+- **Повышает минимально возможный уровень `clarity`** — игрок начинает видеть более точные описания свойств ресурсов. Основное влияние на `clarity` оказывает чистота (`purity`) создаваемых зелий – чем чище зелье, тем быстрее растёт ясность. Фрагменты памяти позволяют достичь более высоких значений, но не заменяют чистоту.
 - **Открывает новые диалоги** с NPC и духами
 - **Разблокирует доступ к новым локациям** (например, тропа на Буян)
 - **Определяет доступные концовки**

@@ -250,12 +250,12 @@

 | Фрагменты | Clarity (примерно) |
 |:---|:---|
-| 0-2 | 0.2-0.3 |
-| 3-5 | 0.4-0.5 |
-| 6-8 | 0.6-0.7 |
-| 9-11 | 0.8-0.9 |
+| 0 | 0.5 |
+| 3 | 0.625 |
+| 6 | 0.75 |
+| 9 | 0.875 |
 | 12 | 1.0 |

+*Примечание: точное значение `clarity` вычисляется по формуле `clarity_effective = 0.5 + 0.5 * (fragments/12)`, но также зависит от `purity` через сглаживание. Таблица даёт ориентир.*

@@ -49,4 +49,9 @@
 - **Knowledge** – система знаний игрока (влияет на Perception).
 - **Progression** – система прогрессии (открытие возможностей).
+- **Respect Score** – накопленное уважение сущностей к игроку (диапазон [-1, 1]). Влияет на отношение (attitude) сущностей архетипов HOST и ANCESTOR.
+- **Memory Fragments** – количество собранных фрагментов памяти (0…12). Влияет на clarity и открывает доступ к концовкам. Не влияет на формулы Process System, только на Perception и нарратив.
+- **Spirit Pressure** – параметр духовного давления, влияющий на некоторые обряды и появление сущностей. Диапазон [0,1]. Изменяется в основном через intent `ritual`.
+
+---