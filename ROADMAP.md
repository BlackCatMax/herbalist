# Дорожная карта: что реально осталось

Обновлено 2026-09-01. Для истории того, что уже сделано, найдено и почему
решено именно так — `CHANGELOG.md`. Спутники: `AUDIT_AND_REFACTORING_PLAN.md`,
`DESIGN_World_State.md`, `META_AUDIT.md`.

**Ядро игры готово и работает**: пайплайн, биомный граф, восприятие,
сохранения, весь бестиарий (§16.2/§16.3/§16.4) с физическими акторами,
капища с типовыми бонусами (тип резолвится от клетки при регистрации),
сутки/луна/сезоны, погода (собственный C++-сигнал), сбор ингредиентов
завязан на сезон/погоду/луну/HarvestStress, Травник (читаемый лог с
фильтром по ингредиенту), Заряна/Буян с презентацией, единый
`IInteractable`, заражение соседних клеток от испорченного полюса
(разрастание поганых мест по сетке, не только по биомному графу),
фольклорная система имён зелий со склонением по падежам, варка на
последовательном согласии/конфликте трав (не симметричное среднее),
градации опасности по числу разных трав (2=база, 3-4=риск нарастает, 5+=
гарантированная катастрофа) и ритуальная (пошаговая, по месту/времени/типу
воды) варка, укрощающая эту опасность, инструмент сбора (голые руки/
железо/медь/кость — множитель качества, не шанса спавна; травы с реальным
фольклорным основанием в тексте карточек портятся железом или выигрывают
от костяного ножа), сад (шесть пристроек — Грибница/Погреб-ледник/Водоём/
Открытая грядка/Тенистая грядка/Пещера — подделывают нишу инженерно, в
обход `AllowedBiomes`, не отменяя его — 41 из 76 карточек компендиума
размечены в живой таблице), Домовой
(хозяин очага игрока, не место на карте — та же инфраструктура Respect/
Landmark, что уже держит Полевика и остальных «хозяев», просто
зарегистрирована напрямую на клетке жилища, не сеется по биому; хорошее
отношение защищает дом, плохое — портит, совсем плохое эскалирует во
второй, более резкий удар), Молва общины (единый накопитель под Диалоги/
Торговлю/Подношения — растёт/падает подношением общине, тот же принцип, что
Restoration/Respect), реанимированный примитив Talk (алгебра команд Q/T/D/S/B
снова полна — инертен в пайплайне, диалог читает Respect/Molva напрямую),
дерево диалогов (данные + фильтрация веток по гейту, один пример — Домовой),
торговля с общиной (курс по ценности предмета, модулируется Molva).
Всё — за автотестами (221/221 зелёных на 2026-09-01, вторая ревизия
"Update docs"/"Update artifacts" закрыта — см. раздел "Главы 18-21 GDD"
ниже). После 178/178
(2026-08-31, Perception/TraceReplay) добавлены: PCG-авторские
сплайн-регионы биома (`ABiomeRegionVolume`, взвешенный спавн ингредиентов
на стыке биомов, замена блочной 5x5-раскраски — 178→190, подробный разбор
только в истории коммитов `PCG-биомы, шаги 1-5`, не в этом файле —
известный разрыв дисциплины ведения документации, см. `CHANGELOG.md`
2026-09-01), `GlobalPerceptionClarity` = якорь + отклик (190→192), Чёрная
роса Заряны (192→196), четвёртый фрагмент ХЛЕБ-СОЛЬ (196→197), базы/
лагеря (197→199), Зеркальце/Клубочек (199, без новых тестов), восемь
артефактов Легендарных (199→206) и три исхода у Буяна (206→208) — все
семь шагов главы 18-21 GDD закрыты, подробности в разделе ниже. Ниже —
то, что осталось.

---

## Баг применения зелья — закрыт 2026-08-30

**Применение уже сваренного зелья на клетку (`UsePotion`) молча превращало
его в золу, стирая реально сваренное качество.** Найдено интеграционным
тестом `Herbalist.PlaySession.GatherBrewApplyEndToEnd` — только он впервые
прогнал "сварить → применить" на РЕАЛЬНО сваренном предмете вместо
сконструированного вручную сценария. Причина: `ApplyPotionToCell`
заворачивает готовый предмет в тот же список `Ingredients`, что и сырые
материалы при варке, а `ComputeApplyResult` не отличал одно от другого —
единственный небольшой предмет без воды в списке безусловно попадал под
правило "обязательность воды" (§05_Systems.md) и становился золой заново.
Фикс — `ComputeApplyResult` распознаёt уже готовый результат по
`IngredientID` (Potion/Ash/BoiledWater) и применяет его `State` напрямую,
без повторного Fold/Morok/Zaryana/Bifurcation. Закреплено двумя тестами:
точечным юнитом (`ProjectHerbalist.PipelineV2.
ApplyAlreadyBrewedPotionKeepsItsOwnState`) и усиленным assert'ом в самом
интеграционном тесте, который это нашёл. 111/111 зелёные.

## Баги финального аудита — оба закрыты 2026-08-30

**Тип капища** больше не захардкожен: `AGridWorldManager::
ResolveShrineTypeForCell` резолвит Пограничное (капище на стыке биомов,
приоритетно) или Лесное/Водное/Каменное по группе биома, иначе
Родовое-фолбэк; `AlchemyTableActor::BeginPlay` вызывает его вместо
константы. **Гейт `bDegrading`-манифестации** теперь освобождает не только
буквальную ось Corruption, а любую ось+направление, согласованные с
испорченным полюсом (`IsAxisConsistentWithCorruptPole`,
`GridWorldManagerEntities.cpp`) — Водяные бесы/Болотные огни/Стукачи по
высокому Distortion, Ржавые духи по низкой Stability. 95/95 зелёные, без
регрессий. Подробности — `CHANGELOG.md`.

## Контент и ощущение игры (не код, либо контентная задача поверх готового C++)

- **Свой персонаж вместо шаблона** — `BP_FirstPersonCharacter` работает
  (движение уже через `AddMovementInput`/`AddYawInput`), но это редакторская
  задача на анимации/темп ходьбы/приседание, не C++.
- **HUD/меню** — паузы, выхода, отображения времени суток нет вовсе, хотя
  часы уже тикают (`GameClockSeconds`).
- **Звук** — только одно поле `HarvestSound`.
- **Локация Буяна** (живая/мёртвая вода) — флаг `bBuyanReached` и
  текстовое объявление готовы (`CheckBuyanCondition`,
  `UI/MemoryRevealWidget.h`), самой локации нет — level design, не код.
- **WBP/Input Action ассеты** — `JournalWidget`/`JournalEntryRowWidget`
  (богатая раскладка с полем на каждую ось) и их Input Action (`JournalAction`)
  не назначены в редакторе. Не блокирует: `JournalLogWidget` (C++-без-WBP)
  уже работает через `ToggleJournalUI()`/Exec без них — заводить WBP имеет
  смысл только если понадобится более богатый экран, чем читаемый лог.

## Механики с открытым дизайном

- **✅ Механика оберегов — первый вертикальный срез закрыт 2026-09-04.**
  Модель таймера: активация даёт GameClockSeconds-экспаери (тот же паттерн,
  что уже `InvisibilityCapExpiryGameSeconds`/`YouthAppleClarityBoostExpiryGameSeconds`/
  `AlkonostSuppressionExpiryGameSeconds`) — "слабый, но непрерывный эффект,
  ограниченный по времени" (прямая формулировка запроса), не разовый
  расходуемый предмет и не бессрочный баф. Состояние живёт на
  `AGridWorldManager` (`GridWorldManagerWards.cpp`), не на контроллере —
  тот же выбор места, что уже держит все остальные Zaryana/Artifacts-таймеры
  (мировая симуляция, не UI-слой), консультируется тем же кодом, что уже
  читает `IsInvisibilityCapActive`. Два эффекта реализованы до конца:
  **BrewBoost** (Громовая стрела) — плоская надбавка к Coherence на варке,
  тем же путём, что бонус капища (`ShrineCoherenceBonus`,
  `PipelineV2.cpp::ProcessApplyCommand`), но без радиуса влияния и заметно
  слабее (`WardBrewBoostCoherenceBonus=0.05` против капищенских до 0.15);
  **EntityConceal** (Плакун-камень) — слабая версия Шапки-невидимки,
  `WardConcealmentRadius=1` против `InvisibilityCapRadius=3`. Оба —
  реактивируемые (не расходуются), владение кристаллом проверяет
  `AHerbalistPlayerController::ActivateWard` по инвентарю (тот же приём, что
  `OfferToCommunity`), новая Exec-команда `ActivateWard <IngredientID>`.
  `WardDurationSeconds=600` (черновое число, длиннее InvisibilityCap/
  YouthApple — слабый эффект должен продержаться дольше короткого
  артефактного пика). **Два кристалла добавлены** (`WardCrystalAppendCommandlet`,
  `-run=WardCrystalAppend`) — Плакун-камень (EntityConceal, пара к уже
  существующей Плакун-траве, тот же слёзный/купальский мотив заговорной
  традиции) и Громовая стрела (BrewBoost, окаменелость-белемнит, народное
  имя "перунова стрела" — намеренно не "чёртов палец", то имя уже занято
  тегом Лещины/шишек Ольхи в компендиуме). Оба — `AllowedBiomes` пуст,
  `GardenNiche=Cave`, `Resilience=0.0` (НЕ 1.0, как у Дубовой коры — сад
  должен уметь тянуть `State→BaseState`), компендиумные карточки —
  `herbalist_docs/Herbalist_Vault/04_Compendium/Минералы/`. Резолв кристалла
  из инвентаря по имени НЕ покрыт автотестом (тот же класс пробела, что уже
  у `TradeWithCommunity` — `IngredientRegistrySubsystem` недоступен через
  `GameInstance` в Editor-мире автотестов) — сам эффект
  (`ActivateWardBrewBoost`/`ActivateWardConcealment`/`IsWard*Active`,
  экспаери, геометрия, надбавка к Coherence через реальный `ExecutePipeline`)
  протестирован напрямую, минуя этот резолв. 311 → 315 тестов, 315/315, два
  чистых прогона, ноль регрессий.
- **✅ Механика оберегов — третий эффект, MorokReduction (Куриный бог),
  закрыт 2026-09-04 (второй заход).** `EWardEffectType::MorokReduction` —
  тот же `Center+Radius` приём, что и `EntityConceal`
  (`ActivateWardMorokReduction`/`IsWardMorokReductionActive`,
  `GridWorldManagerWards.cpp`), но читается из ЕДИНОЙ точки входа восприятия
  `ComputePerceptionDistortion` (`GridWorldManagerEntities.cpp`), не из
  гейта проявления сущностей, и включён по условию `IsNight()` — фольклор
  называет защиту конкретно сна/дурных снов, не восприятия вообще, а ночная
  надбавка Морочников (`NightPerceptionDistortionBonus`) — ровно то
  искажение, которое этот оберег гасит; днём эффект намеренно не
  применяется вовсе, не "тоже, но слабее" (решение и довод — комментарий у
  `EWardEffectType`, `HerbalistCoreTypes.h`). `WardMorokReductionAmount=0.1`
  (черновое число, той же природы, что и остальные — заметно меньше
  `NightPerceptionDistortionBonus=0.25`, оберег приглушает морок, а не
  отменяет ночь целиком) и `WardMorokReductionRadius=1` (то же число, что и
  `WardConcealmentRadius` — тот же класс "слабый радиус вплотную к месту
  активации", не третье придуманное значение). Третий кристалл — **Куриный
  бог** (`WardCrystalAppendCommandlet` расширен третьей записью, тот же
  идемпотентный `AddRow`, `-run=WardCrystalAppend`): камень с естественной
  сквозной дырой, реальный народный оберег ("Божье око"), вешался над
  постелью/в курятнике/хлеву против кикиморы, порчи, дурного глаза и дурных
  снов — компендиумная карточка `herbalist_docs/Herbalist_Vault/
  04_Compendium/Минералы/Куриный бог.md`. Стихия — Вода (не Огонь, как у
  двух первых кристаллов): дыра проточена водой/ветром по тексту поверья,
  без связи с грозой/Купалой.
  **PathReveal (показ троп) — искали ещё раз, реальный фольклорный мотив
  нашёлся, но не подошёл по форме, осталось не реализовано.** "Камень на
  распутье" (бел-горюч Латырь-камень трёх дорог из былины об Илье
  Муромце — стоит на распутье, надпись указывает, что ждёт на каждой из
  трёх дорог) — это ЕДИНСТВЕННЫЙ мифический камень-граница миров в начале
  ВСЕХ дорог, не тип находимого/собираемого камня наподобие плакун-камня
  (реальная купальская практика собирать такие камни) или громовой стрелы
  (реальный амулет-белемнит из каждого дома) — тянуть его под механику
  "вырастить в Пещере, носить в кармане, активировать" значило бы
  придумывать портативную форму мифа, которой в источниках нет. Отдельно
  нашлось реальное народное средство от морока/кружения лешего в лесу
  (вывернуть одежду наизнанку, переобуться) — но это не камень вовсе, не
  тот класс предмета, что несут два уже реализованных кристалла. `EWardEffectType`
  сознательно не получил четвёртого значения — см. подробный довод в
  комментарии у enum'а (`HerbalistCoreTypes.h`). Тест — `Herbalist.Ward.
  MorokReductionOnlyAppliesAtNightNearActivation` (`WardTest.cpp`),
  сравнивает `ComputePerceptionDistortion` до/после активации, ночью/днём,
  в радиусе/вне радиуса — не повторяет таймер/геометрию отдельным тестом
  (тот же код, что уже проверен у `EntityConceal`). 315 → 316 тестов,
  **316/316, два чистых прогона, ноль регрессий.**
- **Словарь фольклорных имён зелий (`HerbalistNameUtils.cpp`) — первый
  черновик, ждёт вашей сверки конкретных слов.** Структура и склонение
  реализованы и протестированы (117/117), сами слова — взвар/дурман/настой/
  вода/варево + 8 эпитетов — подобраны с опорой на реальную фольклорную
  лексику (см. обоснования в комментариях кода), но не сверялись с вами
  построчно. Заменить/добавить слово — правка одной таблицы форм в
  `HerbalistNameUtils.cpp`, архитектуру трогать не нужно.
- **Пороги эпитетов не пересчитаны под новую формулу варки.** После
  докрутки варки (согласие/конфликт, см. `CHANGELOG.md`) мощь зелий заметно
  выросла (диагностика на реальных травах: Багульник+Сон-трава+Полынь в
  речной воде — Magnitude 0.19→0.49), но конкретные Meta-оси (Distortion/
  Purity/Stability/Potency/Corruption) у этой же смеси остались НИЖЕ
  порогов эпитетов (0.6-0.65) — итог по-прежнему "Хилая вода", хотя зелье
  объективно куда мощнее. Пороги эпитетов проектировались до докрутки
  формулы, ни разу не пересчитывались против неё — сознательно не тронуто в
  этом заходе (объём был явно ограничен только формулой варки), но стоит
  разобрать отдельно.
- **Реестр ритуальных рецептов (`Core/Alchemy/RitualTypes.h`,
  `GetRitualRecipeDefinitions()`) содержит ровно ОДИН рецепт** — "Заревая
  вода" (рабочее имя), прямой пример из запроса пользователя (2 травы в
  болотной воде на закате, третья на рассвете). Механизм полностью общий
  (любое число шагов, любая комбинация фазы суток/типа воды/числа
  ингредиентов на шаг) — добавить рецепт значит дописать одну запись в
  массив `Defs`, архитектуру трогать не нужно. **"Роса" из исходного примера
  пользователя не заведена как отдельный тип воды/ингредиента** — ни один
  существующий harvestable её не использует (`ingredients.json`/
  `water_types.json` проверены), первый рецепт приближает её к "рассвет +
  вода среди добавляемого" вместо изобретения новой сущности без
  согласования. Оба — сознательно узкие, ждут вашего разбора: реальные
  названия/составы рецептов и стоит ли всё-таки завести "росу" отдельно.
- **Реальный Ultra Dynamic Weather — C++-сторона готова (2026-09-04), ждёт
  Blueprint-моста.** Плагин физически в проекте, `Get*Intensity` читают
  `Cached*Intensity`, если мост хоть раз позвал `SetWeatherBridgeIntensities`
  (иначе прежний шум без изменений). Осталось — только редактор: расставить
  `Ultra_Dynamic_Sky`/`Ultra_Dynamic_Weather` на уровне и собрать сам
  Blueprint-мост (единственный Blueprint-код во всей интеграции, C++-агент
  не может его тронуть). Чеклист с именами нод — `15_Cycles_And_Shrines.md`
  §15.7, "Мост реализован (2026-09-04)". См. также раздел "UDS/UDW
  физически в проекте" ниже.
- **"Хозяин" растений и ритуал сбора** (Леший/Водяной/Русалки/Хозяин
  Севера...) — почти в каждой карточке компендиума назван, но сознательно
  отложено прямым выбором пользователя 2026-08-29: данных много, часть
  карточек противоречит друг другу (Ольха речной поймы — правило "сухое/
  живое дерево" наоборот, чем у остальных деревьев), и это разошлось бы с
  уже принятым решением, что `Landmark.Respect` меняется только через явное
  подношение (Apply), не автоматически от сбора. Естественная форма без
  нового канала записи, если возьмёмся: читать существующий `Respect` как
  ещё один множитель `Suitability` в `GetRandomResourceForBiome`, не писать
  в него при сборе.
- **`PerceiveClass`** — сейчас искажаются только числа состояния, не сам
  факт "что это за вещь"; перепутать один ингредиент с похожим нельзя.
  06_Progression подразумевает и такое искажение.
- **История/Coherence в метрике мира** — `Distance_итог = Distance ×
  (2 − AverageCoherence)` описана в `15_Cycles_And_Shrines.md §15.5.1`, не
  реализована в коде (сейчас читается голый `Distance`).
- **PCG/материалы** — задел есть (`MPC_WorldStateFields.uasset`), визуальный
  вывод состояния мира в материалы не продолжен.
- **Инструмент сбора (`DESIGN_Community_And_Homestead.md §2.3`) — реализован
  только сам множитель качества, не экономика вокруг него.** Готово: флаги
  `bIronAverse`/`bDelicate` в `FIngredientTableRow`, множитель в
  `GenerateHarvestResult` (голые руки 0.7/железо 1.0/медь-кость 0.9,
  железо-табу 0.3, костяной бонус 1.15), три карточки компендиума с реальным
  фольклорным основанием (Плакун-трава/Чистотел/Медуница) — размечены и в
  тексте, и в живой `DT_IngredientClass` (`ingredient_gathering_and_garden_
  flags.json` + `IngredientGatheringAndGardenPatchCommandlet`, 2026-08-31),
  Exec-команда `SetGatheringTool` на `AHerbalistPlayerController::
  CurrentGatheringTool`. **Не сделано, сознательно вне этого прохода**
  (см. `DESIGN_Community_And_Homestead.md §2.3` — источник артефакт-тира
  курганы/дары хозяев, не торговля): физический предмет-инструмент в
  инвентаре (сейчас переключается только консольной командой, не
  подбирается/не крафтится), серебряный оберег (вторая, независимая ось —
  гасит сигнатуру игрока при сборе, ношимая версия уже спроектированного
  "скрытия"/одолень-травы, не резак вообще, не тронута), штраф `Respect`
  хозяина места за железо на bIronAverse-траве при проявленном хозяине
  (архитектурная граница: `GenerateHarvestResult`/`ProcessHarvestCommand`
  не видят `Landmark`/`Respect`, это состояние живёт на `AGridWorldManager`
  отдельно от `WorldSnap.GridState` — нужен отдельный проход).
  **✅ Проход по всем 76 карточкам компендиума на bIronAverse/bDelicate
  завершён 2026-09-04** (ночная автономная задача, 8 биомов, коммит на
  биом) — новых мотивов не нашлось за пределами исходных трёх карточек,
  оба флага действительно редки в компендиуме, не "руки не дошли".
- **Сад (`DESIGN_Community_And_Homestead.md §2.4`) — реализован сам
  механизм, не экономика построек и не полный охват данных.** Готово:
  `EGardenNiche` (Грибница/Погреб-ледник/Водоём/Открытая грядка/Тенистая
  грядка подлеска/**Пещера**), `GardenNiche` в `FIngredientTableRow`,
  `IngredientRegistrySubsystem::GetRandomResourceForNiche` (параллельный
  кэш кандидатов по нише, та же формула пригодности, что и по биому —
  `PickWeightedResource`, вынесена общей), `AGridWorldManager::GardenPlots`
  + `RegisterGardenPlot`, встроено в `SpawnResourcesInCell` (и транзитивно
  `StartRegeneration`), Exec-команда `SetGardenPlot(X, Y, "mycelium"/
  "cellar"/"pond"/"sunny"/"shade"/"cave"/"none")`. Первая вертикальная
  выборка данных — 10 трав (2 на каждую из 5 пристроек: Дождевик/Бледная
  поганка, Клюква/Сфагновые мхи, Кувшинка/Рогоз, Полынь/Ковыль, Ландыш/
  Медуница), та же таблица/командлет, что и у флагов инструментов выше.
  **✅ Проход по всем 76 карточкам завершён 2026-09-04** (та же ночная
  задача, что и у bIronAverse/bDelicate выше) — `GardenNiche` размечена
  у 41 карточки (было 10): 12 `SunnyBed`, 13 `Pond`, 10 `ShadeBed`,
  5 `Mycelium`, 1 `RootCellar` (новая, первая за весь проход). Остальные
  35 — деревья/кустарники/ягодные кустарнички/кора дерева (граница вне
  всех 5 ниш, установлена и держится консистентно) и яды глухих мест —
  сознательно без ниши, не недосмотр. Подробности по каждому биому —
  `CHANGELOG.md`, семь записей той же ночи.
  **✅ Шестая ниша, Пещера, добавлена 2026-09-04** (прямой запрос
  пользователя) — закрывает тот самый компромисс Грибницы, который эта же
  строка называла ниже ещё вчера: Мухомор/Чага/Белый гриб (микоризные,
  нуждаются в живом дереве-хозяине) переставлены из `Mycelium` в `Cave`,
  в `Mycelium` остаются только настоящие сапрофиты (Дождевик, Бледная
  поганка). Пещера же вводит задел под кристаллы (будущая механика
  оберегов — см. ниже) — сами карточки кристаллов ещё не написаны,
  только место под них. **Не сделано:** сама пристройка — только запись
  в `TMap`, не физическая постройка-актор с визуалом (тот же принцип,
  что у `CurrentGatheringTool` — механизм работает, экономика/визуал нет).
  **Аудит "на аудит" (2026-08-31) нашёл и закрыл реальный пробел**:
  `GardenPlots` не сохранялся вовсе (ни в `FSavedCellState`, ни отдельным
  полем) — перезагрузка молча стирала бы все назначенные пристройки.
  Починено тем же доводом, что уже применён к Molva (§ ниже) — поле в
  `UHerbalistSaveGame` + Save/Load, тест `Herbalist.Community.
  GardenPlotsSurviveSaveLoad`.
- **Домовой (`DESIGN_Community_And_Homestead.md §2.1`) — реализован рычаг
  отношений, не обставление вокруг него.** Готово: `FLandmarkDefinition`
  «Домовой» в уже существующем реестре хозяев места (`LandmarkTypes.h`),
  зарегистрирован напрямую на клетке `AAlchemyTableActor`
  (`AGridWorldManager::RegisterDomovoi`, вызывается из
  `AlchemyTableActor::BeginPlay`, тот же принцип идемпотентности, что
  `RegisterShrine`), не сеется по биому (новый общий флаг
  `bManualRegistrationOnly`, `SeedTestLandmarks` его пропускает). Хороший
  `Respect` — Stability вверх (защита дома, тот же смысл, что у устойчивости
  капищ). Плохой — Corruption вверх (мелкие пакости). Отягощённый порог
  (новые общие поля `AggravatedCurseAxis`/`Rate`/`Threshold` на
  `FLandmarkDefinition`, не специфичные для одного хозяина) — при Respect
  ниже -0.6 добавляется второй удар по Stability, эскалация в домашнюю
  Кикимору (собрана `DESIGN_Brewing_Situations_And_Lore.md`), не отдельная
  сущность, тот же Landmark с более резким проклятием. **Не сделано:**
  обставление (визуальное представление прокачки котла/хранилища — контент,
  не механика, требует редактора), конкретная сумма/материалы для апгрейда
  хранилища (§2.2 «мягкая доступность» остаётся структурным принципом без
  конкретного рецепта).
- **Все шесть систем `DESIGN_Community_And_Homestead.md` реализованы
  (2026-08-31) — Молва, Диалоги, Торговля, Подношения-расширение
  закрывают документ.** Молва — не через генерализованный `FInfluenceSource`
  (Часть V `DESIGN_World_State.md`, по-прежнему не реализован в коде и
  остаётся отдельным, осознанно отложенным архитектурным решением) — свой
  собственный `float Molva` на `AGridWorldManager` + `OfferToCommunity`,
  тем же принципом роста, что уже даёт Restoration/Respect (Gain ×
  (Purity−Corruption)), вне детерминированного пайплайна (community — не
  клетка). Диалоги — `ECommandPrimitive::Talk`/`FTalkCommand` реанимированы
  (структура как до удаления 2026-08-30, инертны в `ExecutePipeline`, тем же
  принципом, что уже `Query`), реальный движок — `Core/Dialogue/
  HerbalistDialogueTypes.h` (реестр деревьев, ветки фильтруются по
  Respect/Molva), один пример — Домовой. **«Разговор с людьми» не
  реализован и не мог быть** — в проекте физически нет NPC-акторов (только
  `AHerbalistEntityActor`/`ALandmarkEntityActor` для хозяев места), дерево
  готово принять человека, как только появится сам актор. Торговля —
  `ComputeCommunityTradeValue`/`TryTradeWithCommunity`, курс от ценности
  (Magnitude/Purity/обратная `RarityWeight`), модулируется Molva. v1 всех
  трёх — Exec-команды (`TalkTo`/`ChooseDialogueBranch`/`OfferToCommunity`/
  `TradeWithCommunity`), не UI-виджеты — тот же принцип, что уже у
  `SetGatheringTool`/`SetGardenPlot`.
  **Аудит сразу после реализации нашёл и закрыл реальный пробел**: `Molva`
  не сохранялась (`HerbalistSaveTypes.h`/`HerbalistSaveSubsystem.cpp`),
  хотя следует тому же правилу "растёт только подношением, без пассивного
  спада", что уже требует сохранения для Shrines/EntityLandmarks — починено
  тем же проходом, закреплено тестом (`Herbalist.Community.
  MolvaSurvivesSaveLoad`, обходит недоступный в автотестах
  `GameInstanceSubsystem`, воспроизводит ту же пару присваиваний напрямую).
  **Не сделано, сознательно вне этого прохода**: физический торговец/UI
  диалога/торговли (только Exec-консоль), реестр диалогов — 1 пример
  (Домовой), торговля с реальными данными не покрыта автотестом
  (`IngredientRegistrySubsystem` недоступен через `GameInstance` в
  Editor-мире автотестов — тот же класс ограничения, что и у
  `FSnapshotService`/`UHerbalistSaveSubsystem`, см. `PIE_VERIFICATION_
  PLAN.md` «Приоритет 3» — сквозная проверка только руками).
  **Аудит "на аудит" (2026-08-31) нашёл и закрыл ещё один реальный баг**:
  `AHerbalistPlayerController::TradeWithCommunity` списывал из инвентаря
  1 единицу независимо от размера стека, хотя `ComputeCommunityTradeValue`
  оценивает ВЕСЬ `Item.Count` предложенного — бесплатная утечка ценности
  при стеке > 1 (достижимо в обычной игре, инвентарь стекается). Починено
  — списывается весь совпавший стек. **Отдельно найден и НЕ закрыт
  (продуктовое решение, не код)**: ветка диалога "Оставить у печи блюдце
  молока" не имеет игрового эффекта — вопреки §1 документа ("Подношения —
  действие, которым Диалог завершается, тот же механизм, что у капищ/
  хозяев места"), `ChooseDialogueBranch` не вызывает существующий
  Apply-механизм подношения хозяину места. Не тронуто намеренно — открытый
  вопрос, чего стоит жест (конкретный предмет? бесплатный символический
  акт?), не факт из документа или карточки — придумывать самому означало
  бы повторить ошибку с "bIronAverse confirmed" из более раннего прохода
  этой же сессии. Подробности обеих находок — `CHANGELOG.md`, запись
  "аудит на аудит".

## Архитектурный долг

**✅ Закрыто 2026-08-30 (два из трёх пунктов).** `FCommandGraph`
переименован в `FCommandBatch` (честное имя — плоский `TArray`, не граф
зависимостей; ни одна из шести команд не зависит от другой в пределах
тика, строить настоящий граф было бы решением несуществующей задачи),
`Talk`/`FTalkCommand`/мёртвый `ExecutionOrder` удалены целиком — не
доведены до реализации, а убраны: `Talk` не имел ни одного вызывающего
пути, и `17_Hero_And_Community.md` прямо отвергает диалог как механику.
Резолв имени/perceived-состояния по виджетам — перепроверено, уже
централизовано (`GetItemDisplayName`, `HerbalistNameUtils.h`), все
проверенные виджеты идут через него, отдельного долга не было. Подробности
— `CHANGELOG.md`, `DESIGN_World_State.md §23`.

- **Полный переход Morok/Zaryana на read-time оверлей** — осознанно
  отложенное архитектурное решение (не баг): тактический фикс (сравнение
  "изменилось ли значение" перед записью в `TargetStateNudges`) уже закрыл
  измеренную проблему (400/400 клеток грязнились от нулевых полей).
  Полная миграция потребовала бы сперва завести сохранение для
  `BiomeGraphSubsystem::Nodes` (сейчас нигде не сохраняется) и тронула бы
  общий цикл релаксации, которым пользуются бистабильность/капища/
  Гнильники/Берегиня — непропорциональный blast radius. Подробности —
  `AUDIT_AND_REFACTORING_PLAN.md §7.1/§7.3`.

## Бестиарий, необязательные хвосты

Основной корпус (§16.2/§16.3/§16.4) закрыт полностью. Два пункта оставлены
как есть осознанно, не как недоделка:
- §16.5 (Вурдалаки/Навьи/Оборотни/Лихоманки/Черти) — общий разлитый ночной
  нудж уже реализован, индивидуальных карточек для них не заводилось: они
  не претендуют на клетку по спецификации, отдельные записи не добавили бы
  наблюдаемого поведения.
- Морочники — не отдельная сущность, а имя для уже реализованного
  `PerceiveValue()`.

## Известные пробелы в тестировании

- Headless UI-тестирование виджетов не настроено — все правки UI этой
  сессии (Травник, Инвентарь, Заряна) проверены сборкой + автотестами на
  логике, но не интерактивно через PIE. Не блокирует, но при следующей
  UI-правке стоит иметь в виду.
- **✅ Варка зелий — закрыто 2026-08-30.** 12 новых тестов
  (`ProjectHerbalist.PipelineV2.Apply*`, `PipelineV2ApplyTest.cpp`) через
  единственную публичную точку входа `Simulation::ExecutePipeline` (сама
  `ComputeApplyResult`/`ComputeIntentCoherence` — `static`, не экспортируются,
  тестируются чёрным ящиком тем же приёмом, что уже применяет
  `PipelineV2Test.cpp` к Harvest): Ash/BoiledWater (точные константы),
  разбавление водой линейное и со штрафом за избыток (точная формула по
  Magnitude — единственная ось, которую Morok/Zaryana не трогают), обе ветки
  Bifurcation (Purified — детерминированно на любом сиде через трюк
  Stability=1.0; Catastrophe — сид откалиброван реальным прогоном), крафт
  vs применение на клетку, передозировка (подключена только к применению на
  клетку, не к крафту — было бы легко упустить), бонус капища и влияние
  биом-графа (сравнительно, не точным числом — Zaryana axis-mix
  нелинейный/tanh), детерминизм (тот же сид → побитово идентичный результат),
  и что `Cmd.Intent.Coherence` вызывающей стороны действительно
  игнорируется. `AlchemySubsystem`/`ApplyAlchemyResult`/`ApplyPotionToCell` —
  тонкая очередь команд без самостоятельной логики, не нуждаются в отдельном
  покрытии (вся реальная арифметика — в `ComputeApplyResult`, уже покрыта).
- **✅ Реальный прогон всей цепочки (не отдельных функций) — закрыто
  2026-08-30.** 3 новых интеграционных теста
  (`Herbalist.PlaySession.*`, `PlaySessionIntegrationTest.cpp`):
  собрать→сварить→применить с настоящим `Tick()` между шагами и настоящим
  `UHerbalistInventoryComponent` (не сконструированным вручную снапшотом),
  сохранение/загрузка посреди сессии на реально пройденном состоянии, и
  несколько циклов сбор→варка→применение подряд без накопления
  несостыковки. По пути нашлась не-баг, но архитектурная слепая зона
  именно этого тестового окружения: `AGridWorldManager::QueueCommand` →
  `Tick()` → `Simulation::FSnapshotService` не тестируем как есть —
  `FSnapshotService::GetSimulationWorld()` ищет мир строго типа Game/PIE
  среди `GEngine->GetWorldContexts()`, а автотесты крутятся в мире типа
  Editor, которого этот фильтр никогда не находит (проверено эмпирически:
  `QueueCommand`+`Tick()` молча не делает ничего, лог "Harvest cell not
  found", несмотря на реально существующую клетку). В реальной игре мир
  всегда Game/PIE — это не баг продакшн-кода. Новые тесты обходят именно
  эту точку (вызывают `Simulation::ExecutePipeline` напрямую и применяют
  результат теми же продакшн-методами, `ApplyStateDelta` на менеджере и
  инвентаре, что вызвал бы `FSnapshotService`, если бы нашёл мир) — сама
  арифметика и применение дельт к живым объектам полностью реальны, в
  обход только поиск мира.
- **✅ Детерминизм симуляции (TraceReplay) — закрыто 2026-08-31.** Считалось
  крупнейшим пробелом, требующим "отдельной архитектурной работы" — при
  ближайшем чтении `FTraceFrame`/`Simulation::ReplayAndCompare`
  (`TraceReplay.cpp`) оказались чистыми структурой и свободной функцией без
  привязки к миру/GameInstance, тем же вертикальным срезом, что и остальной
  пайплайн. `TraceReplayTest.cpp` (5 тестов): подтверждает детерминированный
  повтор, ловит подделанное количество/значение `WorldChanges`, ловит
  повтор с неверным сидом, проверяет кольцевой буфер трассировки. **Попутно
  нашёл и починил реальный баг в самом инструменте верификации**:
  `ReplayAndCompare` сравнивал `InventoryOps` только по количеству, не по
  значению — Harvest-команда (единственный тип, чей результат живёт в
  `InventoryOps`, не в `WorldChanges`) могла разойтись по значению между
  сидами (джиттер сбора), и функция молча сказала бы "детерминировано".
  Найдено буквально написанием теста `DifferentSeedProducesDifferentResult
  AndIsCaught` — он сам не проходил, пока `ReplayAndCompare` не научился
  сравнивать `ContainerID`/`OpType`/`Amount`/`IngredientID`/`Magnitude`/
  `Distortion` каждой операции, не только их число. `PROJECTHERBALIST_API`
  добавлен на `ReplayAndCompare` (не экспортировалась из модуля, тот же
  класс правки, что уже у `FPerceptionService`).
- **✅ `PerceptionService`/восприятие — закрыто 2026-08-31.** Та же
  переоценка сложности, что и TraceReplay выше — `PerceiveRealState`
  оказался чистой функцией. `PerceptionServiceTest.cpp` (6 тестов):
  Distortion=0 → шум ровно нулевой (не "почти", а точное равенство),
  Clarity=1 гасит шум даже при максимальном Distortion (прямая проверка
  заявления 06_Progression/17_Hero_And_Community про Заряну), шум
  масштабируется ЛИНЕЙНО по Clarity на одном сиде (точное деление
  пополам, не "примерно меньше" — RNG вызывается в том же порядке
  независимо от Clarity), структура `ComputePerceivedWorld`/
  `ComputePerceivedInventory` сохраняется (координаты/сид/идентичность
  ингредиента — честные факты, не искажаются).
- **✅ `HerbalistJournalComponent` и `WaterTypeRegistrySubsystem` — закрыто
  2026-08-31.** Полный аудит проекта нашёл, что это были единственные две
  подсистемы вовсе без теста (Journal) плюс уже известный, но дешёвый
  пробел (WaterType) — `JournalTest.cpp` (5 тестов, попутно нашёл и починил
  реальный краш при `MaxEntries <= 0`) и `WaterTypeRegistryTest.cpp` (6
  тестов). Заодно `AHerbalistResourceActor` (не был назван в этом списке
  раньше, но тоже не имел ни одного теста) — `ResourceActorTest.cpp` (5
  тестов, жизненный цикл актора, не дублирует уже полное покрытие формулы
  инструмента в `GatheringToolTest.cpp`).
- Мельче, по-прежнему без тестов: `ProcessTransferCommand`.
  Спонтанный спавн фрагментов памяти Заряны (`TrySpawnStateBasedFragment`)
  получил первое покрытие 2026-09-01 (шаг 3 ниже, только ветка
  `HighCommunityTrust`) — `ShrineRestored`/`LowLocalDistortion` пока
  по-прежнему без прямого теста.

## Известный пробел в документации

`herbalist_docs/Herbalist_Vault/03_Technical/Current/` — заморожена на
уровне прототипа (`Core_Current.md` прямо помечен `based_on:
prototype_2026-04`), не упоминает сохранения/биом-граф/капища/бестиарий/
`IInteractable`/типы контейнеров/заражение вообще, и `World_Current.md`
описывает несуществующую функцию `PropagateToNeighbors` (реально —
`UBiomeGraphSubsystem::PropagateWaves` + заражение по сетке). Требует
осознанного решения — обновить целиком или пометить архивной, не точечной
правки. Также: два крупнейших механизма этой сессии (гейт `bDegrading`
манифестации, заражение по сетке) нигде не описаны в GDD/DESIGN за
пределами `CHANGELOG.md`; и `DESIGN_World_State.md` §24-32 предлагает
архитектуру (`FInfluenceSource`, "оживление" через дрейф/дыхание/пружину),
которая не была построена — реально построенный бестиарий устроен
структурно иначе, параграфы стоит пометить superseded. Найдено финальным
аудитом 2026-08-30, подробности — `CHANGELOG.md`.

**✅ Закрыто 2026-08-31 (по прямому решению пользователя).** `World_Current.md`/
`Alchemy_Current.md` перенесены в `03_Technical/Archive/` — полная
перезапись признана нецелесообразной (риск внести новые неточности выше
пользы), решение принято архивировать, не переписывать. Баннер на месте
файлов обновлён под конвенцию `Archive/` (тот же формат, что у уже
архивных `CONCEPT_ANALYSIS.md`/`DESIGN_Narrative_And_Craft.md`).
`03_Technical/Current/` теперь содержит только `Core_Current.md`,
`Inventory_Current.md`, `UI_Current.md` (не проверялись на ту же глубину
несостыковок — предмет отдельного прохода, если понадобится). Ряд более
мелких несостыковок словаря (`01_Glossary/*`) и `_MOC.md` (статус-таблица
врала на 4 из 8 строк — капища/BiomeState/S_Perceived/Морок-в-восприятии
помечены ❌, хотя реально ✅) починены тем же проходом — подробности
`CHANGELOG.md`, "полный аудит проекта".

## План дальнейших работ (полный аудит 2026-08-31)

Четыре пункта из этого раздела закрыты в тот же день прямым решением
пользователя — см. `CHANGELOG.md` за подробностями каждого:

- ✅ Perception/TraceReplay — оба протестированы (`TraceReplayTest.cpp`,
  `PerceptionServiceTest.cpp`, 178/178 всего), заодно нашёлся и починен
  реальный баг верификации (`ReplayAndCompare` не сравнивал `InventoryOps`
  по значению).
- ✅ `World_Current.md`/`Alchemy_Current.md` — перенесены в
  `03_Technical/Archive/`, полная перезапись отклонена сознательно.
- ✅ Компендиум — Клюква/Сфагновые мхи переставлены с `RootCellar` на
  `Pond` (ближе к прозе карточек, использует существующую нишу, не
  изобретает новую); Русалки поправлены на `level: Низший` в карточке
  (код уже был прав, расхождение было в компендиуме); Болотник/Дух
  Предка/Степные духи/Лесавки помечены "📐 запланировано, не реализовано"
  прямо в карточках, чтобы компендиум не читался как список готового.
- ✅ §17.3/§17.5 `17_Hero_And_Community.md` — непрерывная версия Molva
  принята окончательной, секция переписана под неё (не баннер поверх
  старого текста), трёхуровневая пороговая таблица сохранена только как
  ориентир для будущего диалогового контента, не как спецификация к сборке.

Открытых пунктов от полного аудита 2026-08-31 сейчас не осталось —
дальнейшие задачи снова определяются остальными разделами этого файла
("Механики с открытым дизайном", "Контент и ощущение игры" и т.д.).

## Главы 18-21 GDD: Clarity-прогрессия, Роса, базы, спутники, артефакты, Буян

Пять новых глав в `02_GDD` (`18_Ending`, `19_Rosa_Signal`,
`20_Investment_And_Progression`, `21_Journey_And_Artifacts`,
`22_Lore_Roadmap` — лорная, не код) поверх уже реализованного
вертикального среза сущностей. План (7 шагов) —
`virtual-gliding-octopus.md`; закрытые шаги — подробности в
`CHANGELOG.md`.

- ✅ **Шаг 1 (2026-09-01)** — `GlobalPerceptionClarity` = якорь (монотонный,
  от подлинных фрагментов) + отклик (волатильный, от среднего
  Restoration/Respect минус `MorokHistory` биом-графа, зажат снизу
  якорем). Ложный фрагмент больше не бьёт Clarity напрямую.
- ✅ **Шаг 2 (2026-09-01)** — Чёрная роса Заряны: `GetZaryanaPerceivedState`
  (Слой 1 реальное State её клетки + Слой 3 радиус-влияние капищ/хозяев,
  растущий с Clarity, + честный шум `PerceiveRealState`), `UpdateRosaSignal`
  (Слой 2 — разовая метка, когда её State меняется без прямого действия
  игрока). `ZaryanaCell` по умолчанию садится рядом с домом
  (`AAlchemyTableActor::BeginPlay`), явная расстановка не перезаписывается.
- ✅ **Шаг 3 (2026-09-01)** — четвёртый триггер `HighCommunityTrust`
  (устойчиво высокая Молва, мгновенный порог) + фрагмент ХЛЕБ-СОЛЬ
  (§17.6, точная цитата, спавнится на `ZaryanaCell`).
- ✅ **Шаг 4 (2026-09-01)** — базы/лагеря с нуля: `FHerbalistBase`
  (`Cell`/`Biome`/`DisplayNameID`), `RegisterBase`/`IsValidBrewingLocation`
  (`GridWorldManagerBases.cpp`), `FoundBase` Exec-команда. Крючок
  компендиума для Широколиственного леса — Злыдни, не Гуменник/Овинник/
  Жердяи (см. находку разведки в `CHANGELOG.md`); для остальных 7 биомов
  крючок ещё не подобран (TODO в коде, не выдуман).
- ✅ **Шаг 5 (2026-09-01)** — Зеркальце (`UseMirror`, переиспользует
  `GetZaryanaPerceivedState` целиком) и Клубочек (`UseYarnBall`, тратит
  игровое время = дистанция × `YarnBallSecondsPerUnit`, не телепорт).
  `bHasMirror`/`bHasYarnBall` на контроллере, персистятся. Ни один
  Exec-метод контроллера в проекте не покрыт тестом — не заводил первый
  ради этого шага, см. `CHANGELOG.md`.
- ✅ **Шаг 6 (2026-09-01)** — восемь артефактов (`ArtifactTypes.h`),
  `IsLegendaryManifested`/`IsBereginyaManifested` (новые общие хелперы),
  `TryAcquireArtifact` (честный/обманный путь по реальному/воспринятому
  Purity, Фонарь — только обманом механически, не комментарием).
  Единственный прошитый эффект — Дубинка (расчищает воду для базы,
  `RegisterBase`). Зеркальце/Клубочек переключают прогретое состояние
  дара из шага 5, не заводят новый предмет. Прогрев (Warmth) —
  структурное поле на 0, конкретные пороги НЕ придуманы (§21.5: открытый
  вопрос).
- ✅ **Шаг 7 (2026-09-01) — финал.** Три исхода у Буяна
  (`EBuyanPath::Guardian/TradePlaces/AcceptReality`), `TryChooseBuyanPath`
  (не переигрывается, путь "страж" гейтится
  `GlobalPerceptionClarity`+Молвой, пути 2/3 без порога). Три Exec-команды
  на контроллере показывают только заглушку — финальные тексты трёх
  концовок не написаны (лорная задача `22_Lore_Roadmap.md`, прямой запрос
  пользователя).

**Все семь шагов закрыты.** 178 → 208 тестов за весь проход (из них 190
— PCG-биомы, отдельный проход тем же днём раньше). Открытые, честно не
реализованные куски (детали — `CHANGELOG.md`, "все семь шагов закрыты"):
активный тик прогрева артефактов, семь из восьми конкретных игровых
эффектов артефактов (кроме Дубинки), эффект разоблачения Фонаря, финальные
тексты концовок Буяна, биомные крючки баз для 7 из 8 биомов, момент
сюжетной передачи даров Аграфены, автотесты для Exec-методов контроллера.

### Ревизия "Ending and artifacts" (2026-09-01, коммит `45c7da7` глав)

Пользователь переписал `18_Ending.md`/`21_Journey_And_Artifacts.md`
поверх закрытых семи шагов. Прогресс по новому промпту (детали —
`CHANGELOG.md`):

- ✅ **Шаг 1** — Дубинка убрана (`RegisterBase` снова безусловно
  отклоняет воду для базы); Дубыня переименована в Бабу-Ягу в
  `LegendaryEntityTypes.h` (тот же слот/Pole/оси, только имя и биомный
  комментарий); артефакт "Дубинка" в таблице заменён на
  "Шапка-невидимка"/Баба-Яга. Известный хвост: компендиумная карточка
  `Дубыня.md` не тронута (вне запроса, описывает уже несуществующую в
  коде сущность).
- ✅ **Шаг 2 (2026-09-01)** — тексты трёх концовок Буяна как
  `FMemoryFragmentDefinition` (`BUYAN_GUARDIAN`/`BUYAN_TRADE_PLACES`/
  `BUYAN_ACCEPT_REALITY`, новый триггер `BuyanPathChosen`), доставляются
  из `TryChooseBuyanPath` через `CollectMemoryFragment`. Путь 3 —
  фрагмент написан как незавершённая пограничная мысль (Заряна не
  просыпается в этом исходе, не может ретроспективно вспоминать).
- ✅ **Шаг 3 (2026-09-01)** — шесть эффектов артефактов
  (`GridWorldManagerArtifactEffects.cpp`): Рог (честная диагностика воды),
  Гребень (расходуемый, снимает сущность с клетки — не блокирует
  передвижение, механики непроходимости в проекте нет), Молодильное
  яблоко (расходуемое окно нулевого шума росы), Шапка-невидимка (не
  расходуется, подавляет НОВЫЕ проявления по всей сетке), Камень-оберег
  (пассивный, гасит один неудачный бросок Bifurcation через новое поле
  `FApplyCommand::bBifurcationCharmActive`, не прошит для ритуалов —
  открытый вопрос). Фонарь без изменений.
- ✅ **Шаг 4 (2026-09-01) — решено не реализовывать в этом проходе.**
  Промпт делал шаг опциональным ("если решишь их для этого прохода").
  Warmth остаётся структурным полем на 0 (см. `ArtifactTypes.h`) — сам
  §21.5 называет пороги прогрева "открытыми вопросами следующего
  прохода", а активный тик пришлось бы либо изобретать числа самому
  (то же, от чего уже дважды в этой ревизии сознательно отказались —
  ClarityGain в шаге 2, пороги Рога/окон в шаге 3 — там числа хотя бы
  черновые и заявлены как таковые; здесь глава прямо говорит "не мой
  звонок"), либо тянуть ту же неопределённость, что уже у Фонаря
  (асимметричный источник прогрева). Не реализовано — не забыто.

**Ревизия "Ending and artifacts" закрыта.** 208 → 212 тестов за три шага
(шаг 4 — решение без кода). Полный прогон: **212/212, ноль регрессий.**

### Вторая ревизия "Update docs"/"Update artifacts" (2026-09-01, закрыта)

Промпт заявлял baseline 208/208 — фактическое состояние 212/212 (не
отражал уже закрытую ревизию выше). Шаги 1/3 промпта — уже сделаны той
ревизией, не переделаны.

- ✅ **Шаги 0-2 (2026-09-01)** — подтверждён реальный baseline (213/213
  после добавления шага 2); Баба-Яга/Шапка-невидимка проверена тестом
  как проходящая через `TryAcquireArtifact` без специального случая
  (код уже был верен).
- ✅ **Шаг 4 (2026-09-01)** — пять новых ключевых фрагментов (`17_Hero_
  And_Community §17.7`): TISHINA_LESA/OJIDANIE_BURI/NEUDOBNAYA_PRAVDA
  переиспользуют существующие триггеры с биомным фильтром; NIT_MATERI —
  новый событийный триггер (Клубочек получен); NE_POKHVALILA — **открытый
  вопрос**, Смешанный лес не держит Malign-Легендарного в реестре,
  приближено через "Баба-Яга сейчас проявлена" вместо точного смысла главы.
  Попутно найдена и починена контаминация между State-триггерами в тестах
  (клетки Тайги по умолчанию ниже порога Distortion — ловили не тот
  фрагмент первым вызовом).
- ✅ **Шаг 5 (остаток, 2026-09-01)** — Фонарь, прогретая версия:
  `UseLanternDisclosureOnCell`, честно показывает `Cell.State.Meta`
  напрямую, требует `IsArtifactWarmed("Фонарь")`.
- ✅ **Шаг 6 (2026-09-01)** — прогрев, вариант C: зелье нужного типа
  (черновые эвристики по осям `FRealState`) + варка в родном регионе при
  проявленной сущности (переиспользован `IsLegendaryManifested`)
  накапливает `Warmth` в `RunSimulationStep`. Клубочек — отдельная
  проверка (ингредиенты из ≥2 биомов через `AllowedBiomes`). Известный
  пробел: накопление внутри `RunSimulationStep` не тестируется напрямую
  (тот же класс ограничения автотест-раннера, что уже у Камень-оберега/
  подношений капищу — `PlaySessionIntegrationTest.cpp` документирует
  причину).
- ✅ **Попутная архитектурная правка** — Зеркальце/Клубочек больше не
  "неполный дар Аграфены второпях" (§21.2 сняла эту рамку): честная
  добыча даёт БАЗОВЫЙ предмет (`bHasMirror`/`bHasYarnBall`), прогрев —
  отдельно, через тот же Warmth-механизм §21.4, что и у остальных шести.
  Мёртвые поля `bMirrorWarmed`/`bYarnBallWarmed` удалены (были только на
  запись).

**Ревизия закрыта.** 212 → 221 тест за пять коммитов (шаги 0-2 одним
коммитом, 3/1 уже были сделаны прошлой ревизией). Полный прогон:
**221/221, ноль регрессий.** Открытые вопросы, зафиксированные не
решёнными окончательно: триггер NE_POKHVALILA (Смешанный лес без
Malign-Легендарного в реестре), Гребень/Шапка без реальной механики
непроходимости/зоны (тот же класс упрощения, что и раньше), Warmth-
накопление без прямого теста (ограничение автотест-раннера, не пробел
в покрытии кода).

### Ревизия глав 16/17/18/19/21/22 — первый кадр, обман Болотного царя,
### перья вещих птиц, числа прогрессии (2026-09-02)

Baseline подтверждён: 221/221. Итог: **221 → 233 теста, ноль регрессий**
(один комбинированный build+test прогон на всю ревизию — цикл сборки/
тестов в этом окружении оказался быстрым, секунды, не десятки минут,
поэтому все четыре шага реализованы и проверены одним прогоном, а не
четырьмя последовательными; см. CHANGELOG.md за детальной разбивкой по
шагам).

- ✅ **Шаг 1 — первый кадр (`19_Rosa_Signal §19.4a`)** — вертикальный
  срез: `SeedRosaCorruptedCircle` сеет реальную Distortion/Corruption-
  порчу в радиусе `RosaCorruptedCircleRadius` вокруг стартовой
  `ZaryanaCell`, спадающую к краям, при первом (и только первом)
  размещении. Постановка (камера/тайминг/партиклы уползающего облака) —
  заглушка с комментарием на главу, в проекте нет системы катсцен.
- ✅ **Шаг 2 — сцена обмана Болотного царя (`21_Journey_And_Artifacts
  §21.3`)** — `TryLureSwampTsarWithPotion`: обманное зелье-приманка рядом
  с проявленным Царём, по-настоящему вероятностный исход (не
  детерминированный порог, как у честного/обманного подношения), Шапка-
  невидимка гарантирует исход, не заменяет приманку.
- ✅ **Шаг 3 — перья вещих птиц (`16_Entity_Manifestation §16.4`)** —
  новый файл `GridWorldManagerProphetFeathers.cpp`. Гамаюн реализовал
  сам недостающий эффект-мишень ("усиленное Зеркальце — иногда
  пророческое", раньше только направление без кода) и его гарантию;
  Алконост масштабирует подавление Шапки с (фактически) всей сетки на
  один биом; Сирин — честное разовое чтение при Malign-спайке; Жар-птица
  — единственный постоянный эффект, новое поле `FGridCell::bEternallyPure`
  исключает клетку из релаксации/заражения соседей/всех трёх рангов
  проявлений навсегда.
- ✅ **Шаг 4 — числа прогрессии (`20_Investment_And_Progression §20.3`)**
  — диапазон отклика (±0.2) и Warmth-пороги (0.2/1.0) оставлены как есть
  с явным обоснованием (не переигрывались вслепую); реально новое —
  скорость сходимости отклика: раньше применялся мгновенно, теперь
  сглажен экспоненциальным Lerp (`ClarityResponseLerpRate=0.002`,
  ~3.9 игровых суток до 95% сходимости), убирает заметный скачок Clarity
  за один пятисекундный опрос, сохраняя структурную гарантию "не ниже
  якоря" (она и так не зависела от диапазона/сглаживания).

### Механизм "выдержано N секунд" (2026-09-02)

По направлению, найденному разведкой знакомства с проектом ("три места с
явным комментарием об отсутствующем механизме длительности"), реализовано
через `-луп-`-воркфлоу. `HerbalistCore::Math::TickSustainedCondition`
(`HerbalistCoreMath.h`) — временной аналог `PassesHysteresisThreshold`.
Три триггера памяти Заряны, чей текст главы дословно требует
длительности, переведены с мгновенного порога на настоящее "выдержано":
`TISHINA_LESA`/`OJIDANIE_BURI` (per-клеточные аккумуляторы) и `KHLEB_SOL`
(один скаляр, Molva). Путь Стража Буяна сознательно НЕ тронут — его
собственный текст (`18_Ending §18.2`) нигде не требует длительности,
явно назван "наброском направления".

Опросные, не персистятся (тот же класс поля, что `FragmentStateCheckAccumulator`).
Найдена и починена контаминация тестов (клетки Тайги по умолчанию
Distortion=0 повсеместно, не только на тестируемой клетке — множественные
клетки копили hold-time параллельно). 233 → 236 тестов, **236/236, ноль
регрессий.**

### Шапка-невидимка — настоящая зона (2026-09-02)

Найдена разведкой знакомства с проектом и почищена по прямому запросу:
`IsInvisibilityCapActive()` был безусловным на всю сетку, несмотря на
текст главы ("выводит клетку/зону"). Теперь — Chebyshev-радиус
(`InvisibilityCapRadius`) вокруг клетки активации, определяемой
контроллером через `WorldPositionToCell` в момент применения. 236 → 237
тестов, **237/237, ноль регрессий.**

### Компендиум трав: проход продолжен + найден и починен баг двух командлетов (2026-09-04)

Автономная ночная задача (выбрана самостоятельно из списка "открытого
дизайна" — не изобретает числа, только категориальные флаги по реальной
улике в тексте карточки). Тундра (6 карточек) проверена целиком, мотива
нет ни у одной — честный отрицательный результат. Степь (8 карточек)
размечена: 4 → `SunnyBed`, 1 → `RootCellar`, 2 ядовитых сорняка гиблых
мест — сознательно без флага.

**Побочная находка важнее самой разметки**: первый же прогон
`-run=IngredientGatheringAndGardenPatch` уронил несвязанный тест
(`Herbalist.ArtifactInventory.DataTableRowsResolveDisplayNames`) — 5 рядов
с пробелом в имени (`Молодильное яблоко`, все 4 `Перо *`) молча терялись
при ЛЮБОМ прогоне этого командлета (и его близнеца
`IngredientHarvestWindowPatchCommandlet`) из-за полного JSON-роундтрипа
таблицы. Оба переписаны на точечную правку через `Table->FindRow<...>`)
— трогают ровно те ряды, что в патче, остальные байтово нетронуты.
Подробный разбор диагностики — `CHANGELOG.md`.

**✅ Проход завершён 2026-09-04.** Все 8 биомов (76 карточек) проверены
построчно, коммит на биом. Итог и точная разбивка — запись "Компендиум
трав: проход завершён" в `CHANGELOG.md`, и раздел "Контент и ощущение
игры" выше (§2.3/§2.4).

### UDS/UDW физически в проекте — C++-сторона моста погоды готова (2026-09-04)

«Я залил UDS в проект, реализовать и проверить луну/сезоны/погоду/сутки».
Архитектура была решена заранее (`15_Cycles_And_Shrines.md §15.7`):
сутки/луна/сезон остаются 100% C++ (визуально пушатся В UDS, обратно не
читаются — трёхпольный календарь/7-суточная фаза луны не обязаны совпасть
с астрономией плагина), погода — наоборот, UDW становится источником
истины. Добавлены `Cached*Intensity`/`SetWeatherBridgeIntensities`/
`bWeatherBridgeActive` на `AGridWorldManager` — `GetWindIntensity` и т.п.
читают кэш, если мост хоть раз позвал сеттер, иначе прежний шум без
изменений (ноль регрессии для уровней без UDW). 310→311 тестов,
**311/311, два чистых прогона.**

**Осталось (редактор, не C++):** расставить `Ultra_Dynamic_Sky`/
`Ultra_Dynamic_Weather` на уровне и собрать один маленький Blueprint-мост
— единственный Blueprint-код во всей интеграции, C++-агент физически не
может тронуть чужой граф. Полный чеклист с найденными строковым поиском
именами нод (`Wind Intensity`/`Rain Intensity` подтверждены; отдельного
`Snow Intensity` не нашлось — видимо, решается по температуре) — в GDD,
подраздел "Мост реализован (2026-09-04)".

### Отрастание поресурсно + счётчик ресурсов в Cell info (2026-09-04)

"Почему некоторые квадраты полностью пустые внутри биома?" + в том же
логе `Cell (3,4): ресурсов 123`. Оба симптома — одна слепая отладочная
строка (`GetSelectedCellInfo()` печатала только первый ресурс клетки, не
счётчик) поверх честно широкого `Min/MaxResourcesPerCell` региона: пустые
клетки — реальный ноль из `RandRange(Min, Max)`, не потеря. Строка теперь
показывает `Resource=bol_12 x3`.

Заодно — реальный баг отрастания: `OnResourceCollected` звал
`StartRegeneration` только при `Cell.ResourceActors.Num() == 0`, и клетка
с несколькими ресурсами почти никогда не пустела до конца — отрастание
для неё не срабатывало вовсе. По запросу "а можно отрастание сделать
поресурсно, а не по клеткам?" — теперь каждый сбор запускает свой таймер
на один ресурс (`SpawnOneResourceInCell`, общий шаг с первичным
заселением). Дефолт времени поднят с 10 СЕКУНД (отладочное значение) до
7 минут (`ResourceRegrowthTime`/`ResourceRegrowthTimeSeconds`) — запрос
"минут 5-10", середина диапазона. 306→310 тестов, **310/310, два чистых
прогона.**

### Акторы подписаны в аутлайнере (2026-09-03)

"При выделении акторов в PIE не понятно, что это за актор, заглушка одна
на всех". Меш-заглушка одна на весь бестиарий, движковое имя вида
BP_..._C_37 не говорит ничего. Добавлен SetHerbalistDebugLabel
(HerbalistActorLabel.h): ресурс -> "ste_06 (12,34)", сущность ->
"Гнильники (7,9)", фрагмент -> "Фрагмент TIKHOE_MESTO [ложный] (65,54)".
Только редактор (SetActorLabel под WITH_EDITOR), игрок метку не видит.

### Плотность существ: минимальная дистанция по виду (2026-09-03)

"Слишком много существ, спавнятся каждые 3 метра". Регулировать было
нечем: потолок задавала сетка (одно поле ManifestedEntityID = одна
сущность на клетку), а порог отвечает за "где возможно", не за "как
редко". Добавлен FAmbientEntityDefinition::MinSpacingMeters -- клетка
молчит, если проявление ТОГО ЖЕ вида уже есть в радиусе. По виду (чтобы
Гнильники кишели, а Лихо было редким), на определении (выбор
пользователя -- гибкость), в метрах (грид-независимо).

Дефолт 0 = выключено: первая версия с 30 м уронила давний тест Межевых --
в тестовом мире клетка 1 м и сетка 20 м поперёк, любая дистанция от ~20 м
подавляет там всё. Механизм включается поштучно на виде.

**✅ Проставлено по всем 33 видам 2026-09-04** (прямой запрос
пользователя). Прямых текстовых улик о численности почти нет (6 из 33
карточек) -- три группы по факту опоры, не 33 придуманных числа: 0 м у
явного роя (Гнильники/Суховейки), 30 м у явной территориальности
(Курганники/Курганные огни/Чащобные духи/Жердяи), 15 м -- средняя ставка
у остальных 27. Новый `AmbientEntitySpacingPatchCommandlet`
(`-run=AmbientEntitySpacingPatch`), точечная правка через `FindRow`, не
полный JSON-роундтрип (тот же урок ночи раньше). Заодно нашёлся и
починен реальный баг именно в тесте Межевых: тестовая "чужая" клетка-
одиночка граничит СРАЗУ с четырьмя сухопутными соседями, не с одним --
раньше это было неважно, с боевым `MinSpacingMeters` все четыре стали
конкурентами за проявление, и тест угадывал результат, а не проверял
его. Подробности и точная разбивка по видам -- `CHANGELOG.md`. 311/311,
два чистых прогона.

### Трансформация ресурсов "как у PCG в ноде Transform" (2026-09-03)

Запрос пользователя после починки джиттера: равномерный скейл, поворот
Z 0..360°, завал X/Y ±5°, затухание плотности/скейла к границе региона,
доп.смещение позиции. Настройки живут на ABiomeRegionVolume (не
глобально) -- диапазоны Min/Max, из которых на каждый актор берётся своё
число. Скейл дефолтит 0.85/1.15, Yaw/Tilt -- ровно запрошенные числа,
смещение/затухание выключены по умолчанию (запрошены как "возможность").

Затухание -- приближение (центроид сплайна + макс. радиус до вершины),
не точный inset многоугольника; для выпуклых регионов достаточно точно.
Без региона (блочный фолбэк) -- нейтральная трансформация, поведение не
меняется. 5 новых тестов в BiomeRegionPlacementTest.cpp.

### Тот же тайлинг у существ -- джиттер был прибит к сантиметрам (2026-09-03)

Пользователь: "то же касается существ". EntityManifestationJitterRadius
был абсолютным числом см (30.0f), настроенным на дев-масштабе -- после
перехода на 5x5 км те же 30 см стали 3% клетки, решётка ещё заметнее,
чем у ресурсов. Переведено в EntityManifestationJitterFraction (доля
CellSize, 0.3 по умолчанию -- меньше ресурсного 0.5, сохраняет замысел
"сущность-якорь"). Вынесено в GetEntityManifestationJitterRadius(),
симметрично GetResourceJitterRadius().

### Тайлинг в рассадке ресурсов: джиттер покрывал 36% клетки (2026-09-03)

Пользователь заметил решётчатый паттерн и спросил, не стоит ли увезти
всю рассадку ресурсов на PCG. Причина оказалась в одной константе, не в
архитектуре: джиттер вокруг центра клетки был CellSize*0.3 -- квадрат
покрытия 36% площади, пустое кольцо по краям, на масштабе мира читалось
как решётка. Поднят до CellSize*0.5 (jittered grid, Cook 1986) --
покрывает клетку целиком. Вынесено в GetResourceJitterRadius(), единый
источник истины для трёх мест, что раньше дублировали формулу.

Архитектурное решение: остаться на C++, не переезжать на PCG -- вся
логика гейтинга (HarvestStress/сезон/высота/ниши сада) завязана на
живое состояние симуляции, переносить её на граф ради бага с
пятиминутным решением было бы неоправданно.

### Проявленные сущности не деспавнились с чанком (2026-09-03)

Живой playtest 0.8 нашёл третий, независимый баг того же дня:
«растительность режется чанками отлично, но не деревья-заглушки для
entities». Ресурсы усыпляются симметричной парой SetChunkResourcesActive
(true/false), у проявленных сущностей такой пары не было -- заспавненный
актор оставался в мире бессрочно после деактивации чанка. Добавлен
DespawnChunkEntities, симметричный вызов из CatchUpActivatedChunks;
Cell.ManifestedEntityID не трогается, актор пересоздаётся сам при
реактивации через уже существующий SyncManifestedEntityActor.

**Осталось по 0.8:** проверить догон (собрать -> уйти -> вернуться) и
`ke * ShowShrines` при включённом стриминге.

### Высотный пояс не работал: единственный кандидат обходил гейты (2026-09-03)

0.5/0.6 плана закрыты (вода как регион -- подтверждена типом воды по
биому; плотность по регионам -- подтверждена пользователем).

0.7 (высотный пояс) нашёл реальный баг, причём двойной: и
`PickFromBiomeWeightedCache`, и `GetRandomResourceForNiche` имели
короткий путь "один кандидат в биоме/нише -- отдать сразу", который
обходил PickWeightedResource целиком -- вместе с ним ВСЕ гейты (высота,
сезон, луна, погода), не только высотный. Плюс запасной фолбэк внутри
самого PickWeightedResource ("TotalWeight~0 -> Candidates[0] всё равно")
отменял высотный гейт даже когда функция вызывалась, поскольку высота --
единственный по-настоящему жёсткий (не смягчающий) множитель в системе.
Оба убраны/поправлены. Существующие тесты не ловили это, потому что
всегда держали рядом контрольную карточку без пояса.

### Плановая проверка PIE: 0.1-0.4 закрыты, `Cell info` слеп к воде (2026-09-03)

Пункты 0.1 (окружение), 0.2 (масштаб), 0.3 (превью спавна), 0.4 (капище)
плана проверки закрыты по логам/подтверждению пользователя.

Попутная находка на 0.5 (вода как регион): `GetSelectedCellInfo()` --
строка, которую печатает правый клик/`Info()` -- никогда не проверяла
`Cell.bIsWater`, только `ResourceActors`. Вода не даёт ресурсного актора,
поэтому водная клетка печаталась как `Resource=None`, неотличимо от
пустой суши. Починено: печатает `Water(<WaterTypeID>)`.

### Периодическая просадка -- второй, независимый источник (2026-09-03)

После правки ForEachActiveCell пользователь сообщил новый симптом:
"просадка каждую секунду, между ними 60-70 фпс". Причина отдельная --
`UPerceptionComponent::TickComponent` безусловно, дважды в секунду, делал
полный захват сетки + перцепцию ВСЕХ 250 000 клеток (`Grid->CaptureState()`
+ `ComputePerceivedWorld()`, около полумиллиона TMap-вставок за вызов) --
хотя `AGridWorldManager::GetPerceivedWorld()` не имеет ни одного читателя
в проекте. Мир больше не считается в тике; `GetPerceivedWorld()` считает
по требованию, лениво, тем же const_cast-приёмом, что уже у
`UIngredientRegistrySubsystem::EnsureLoaded`.

**Осталось:** проверить в PIE, что стало плавно на всём протяжении, не
только между известными пиками -- оба источника теперь должны быть закрыты.

### Три горячих прохода наконец стримят стоимость (2026-09-03)

Жалоба «очень низкая производительность на масштабе 500x500» указала на
недоделанное обещание более раннего юнита стриминга: `RegenerateCellParameters`/
`ApplyBiomeInfluences`/`UpdateEntityManifestations` честно проверяли
`IsCellActive` перед дорогой работой, но сам обход по-прежнему шёл по ВСЕМ
250 000 клеткам каждый тик/такт, просто чтобы отбраковать почти все.
Новые `ForEachActiveCell`/`ForEachCellInChunk` идут прямо по активным
чанкам — O(активных клеток), не O(всей сетки). Все три прохода переведены,
формулы внутри не тронуты.

Первая версия сломала честный тест (`RadiusGatesCellsByChunkDistance`):
полагалась на кэш, живой только после `Tick()`, а прямой вызов без тика
(легитимный юнит-тест) получал ноль клеток. Правка — общая геометрия
`ComputeChunksWithinRadius`, не зависящая от Tick. Новый тест
`ForEachActiveCellDoesNotDependOnTick` — регрессия на этот сценарий.

**Осталось:** проверить на глаз плавность в PIE на масштабе 5x5 км.

### Пустой мир: реестр не успевал загрузиться (2026-09-03)

Вопрос пользователя «в дебаге есть, в игре нет акторов» вскрыл причину,
которую предыдущий разбор не нашёл. Реестр ингредиентов грузился из
`GameMode::BeginPlay`, а тот идёт ПОСЛЕ размещённого на уровне
`BP_GridWorldManager` -- спавн ресурсов читал пустой реестр и молча не
ставил ничего **во всём мире**, включая покрытые биом-регионами десять
процентов. Вчерашний диагноз (90% клеток вне регионов) верен как отдельная
задача уровня, но причиной пустоты не был.

Реестры ингредиентов и типов воды теперь загружают таблицу сами при первом
чтении -- тот же приём, что уже у реестров сущностей. Порядок BeginPlay
больше ни на что не влияет.

**Осталось:** проверить глазами в PIE (признак в логе -- `loaded 89
ingredients` ДО `Cached ... cell heights`) и всё-таки расширить
биом-регионы.

### Ранг трёх существ и молчащий сбор (2026-09-03)

Расхождение ранга, найденное сверкой, разрешено **переносом строк**, а не
правкой карточек (решение пользователя: истина в документации и коде, не в
ассетах). Курганники, Жердяи и Курганные огни вернулись в Низший ранг:
документация называет их так четырьмя независимыми местами против одной
фразы прозы §16.2, которая теперь зачёркнута с датой. Правлены оба
генератора таблиц, иначе пересоздание с нуля вернуло бы расхождение.
Расхождений ранга больше нет; Низший сошёлся 33/33.

Разбор PIE-лога показал, что жалоба «сбор не сработал» -- не про код сбора:
90% клеток уровня лежат вне всех `ABiomeRegionVolume`, а там контент не
спавнится вовсе. Настоящий дефект -- молчание: у `Harvest()` было четыре
безмолвных `return`, у `Inventory()` -- четыре. Теперь каждый называет
причину, а самый частый ещё и указывает, что клетка вне регионов.
**Осталось на стороне уровня:** расширить биом-регионы -- пока девять
десятых мира пусты по построению.

### Заготовки бестиария и фикс висящей травы (2026-09-03)

Найденный в PIE баг: трейс посадки брал первое попадание сверху, то есть
крону дерева -- растение садилось на неё, а проверка занятости этого не
ловила (над кроной пусто). Теперь ищется именно ландшафт. Сверка научилась
искать карточку по всем трём таблицам рангов и тем самым переклассифицировала
три «пропажи» в расхождение РАНГА (Жердяи/Курганники/Курганные огни
реализованы Основным, карточка говорит Низший) -- заготовки по прежнему
отчёту наделали бы дублей. Добавлены 4 настоящие заготовки с нулевыми
ставками эффекта (числа тюнинга парсер не выдумывает), удалена устаревшая
карточка Дубыни. Растения и Низший ранг сошлись полностью.

### Сверка компендиума с таблицами (2026-09-03)

`-run=CompendiumAudit` читает 154 карточки и сами DataTable-ассеты и
сравнивает поле с полем, ничего не записывая. Растения сошлись полностью
(76 карточек, ноль расхождений в числах) -- таблица не разъехалась с
компендиумом. Бестиарий: 9 карточек без строки, причём Дубыня -- случай,
где устарела карточка, а не таблица (существо изъято из дизайна). По ходу
отладки сверка сама вскрыла конвенцию именования (ключ строки -- поле `id`
карточки) и две неподдержанные формы YAML. Запись «карточка -> таблица» не
делалась намеренно: в таблицах живёт геймплейный тюнинг, которого в
карточках нет, и генерация «в лоб» его бы уничтожила.

### Обратная связь «симуляция -> PCG-граф» (2026-09-03)

Новый PCG-узел **Get Herbalist Grid** отдаёт графу сетку облаком точек с
атрибутами состояния (Distortion/Corruption/Purity/Stability/HarvestStress/
ShrineRestoration/Biome/bIsWater/ManifestedEntity). Связь с PCG перестала
быть односторонней: растительность теперь может вырастать ИЗ симуляции --
редеть на вытоптанном, гуще у ухоженного капища, гибнуть под Мороком --
обычными нодами графа, без правки C++ на каждое правило. Работает по
живому состоянию мира (генерация в рантайме); в редакторе до BeginPlay
клеток нет, и узел честно об этом говорит. 280 → 282 теста, **282/282**.

### Высотный пояс произрастания (2026-09-03)

Карточка ингредиента может расти только в заданном диапазоне высот
(`bUseAltitudeRange` + мин/макс + мягкий край). В отличие от окон
сезона/луны это жёсткая маска: вне пояса вес ровно 0, потому что высота --
свойство места, а не момента. Когда ландшафта нет и высота неизвестна,
гейт не применяется вовсе, иначе он молча вычистил бы половину таблицы.
277 → 280 тестов, **280/280**.

### Размещение ресурсов: поверхность, занятость, превью в редакторе (2026-09-03)

Точка спавна садится на поверхность трейсом (при клетке 10-25 м растение
иначе висело в воздухе или тонуло в склоне) и проверяется на занятость
сферическим оверлапом -- камень или дерево теперь отбраковывают точку, а
при отсутствии свободного места клетка честно остаётся пустой. Две кнопки
`CallInEditor` на менеджере рисуют превью точек прямо в редакторе, без
запуска игры: зелёные свободны, красные забракованы. Тест поймал до
коммита, что игнорировать надо весь класс `ALandscapeProxy`, а не один
закэшированный `ALandscape`: в World Partition ландшафт разбит на десятки
прокси, и без этого занятой была бы каждая точка мира. 275 → 277 тестов,
**277/277**, два чистых прогона.

### Стриминг сетки: юниты 2-3, догон и материализация (2026-09-03)

Догон при активации: релаксация за всё время простоя одним шагом, точно (а
не приближённо) -- `MoveToward` линеен и останавливается у цели. Тест
сравнивает с эталоном непрерывного прогона и поймал до коммита неверное
правило первой встречи, из-за которого дальний мир стоял бы замороженным.
Материализация ресурсов по чанкам: клетка помнит, что на ней росло
(`DormantResourceIDs`), сейв это помнит тоже, а акторы PCG-графа сетка не
трогает -- для этого заведён отдельный `bSpawnedByGrid`, потому что
`bInitializedFromCode` выставляется и на PCG-пути. 271 → 275 тестов,
**275/275**, два чистых прогона. План стриминга закрыт целиком.

### Стриминг сетки, юнит 1: активное множество чанков (2026-09-03)

Дорогие полные обходы (релаксация, проявления, влияние биом-графа) теперь
считаются только в чанках вокруг источников стриминга, которые сетка берёт
у самого партишена (`UWorldPartition::GetStreamingSources`). Данные всех
клеток при этом остаются в памяти -- стримится стоимость, а не данные,
поэтому мировые сканы (посев якорей, Буян, проверка проявленности
Легендарного) продолжают видеть весь мир и не потребовали правок. По
умолчанию `ActiveChunkRadius=-1` -- активны все чанки, поведение как
раньше. 268 → 271 тест, **271/271**, два чистых прогона.

### Такт проявлений и PCG-расстановка ресурсов (2026-09-03)

Подготовка к миру 5×5 км. `UpdateEntityManifestations` (самый дорогой
проход: вся сетка × 28 карточек, каждый кадр) переведён на такт 10 Гц
через `EntityManifestationIntervalSeconds` — накопленное время передаётся
внутрь, поэтому ставки эффектов не искажаются. Расстановка ресурсов
PCG-графом стала полноценной: актор без `Init()` добирает данные строки по
`IngredientID`, регион можно отдать графу целиком
(`bSpawnResourcesFromGrid=false`), а отрастание собранного воспроизводит
класс из новой колонки `ResourceActorClass`, а не жёстко базовый.
265 → 268 тестов, **268/268**, два чистых прогона.

### Капище — отдельное место, развязка с котлом (2026-09-02)

Пересмотр решения v1: капище больше не «там, где стоит котёл». Новый
плейсед-актор `AShrineActor` (тип от клетки или заданный руками, плюс
`InitialRestoration` для уже ухоженного капища), котёл капище не
регистрирует вовсе — надбавка к Coherence теперь даётся только если котёл
в радиусе капища, то есть место варки стало выбором. Домовой и Роса
Заряны остались на котле (очаг, не капище). Попутно найдена настоящая
дыра в изоляции тестов (акторы через `TActorIterator` находили настоящий
`BP_GridWorldManager` с уровня вместо тестового) и тест, который из-за
неё проходил по ложной причине — подробности в CHANGELOG.md.
261 → 265 тестов, **265/265**, два чистых прогона.

### Подготовка к работе в редакторе (2026-09-02)

Ревизия всех 13 классов акторов перед переключением с кода на движок:
готово почти всё (компоненты, сплайн-волюмы с настройками плотности, 6 WBP
с назначаемыми хуками, 12 Input Action). Закрыты два места, где editor-
работа упёрлась бы в код: у `FMemoryFragmentDefinition` появился
`ActorClass` (спавн фрагмента был жёстко прибит к базовому классу без
меша — сделать его видимым из редактора было нельзя в принципе), и
заведены две настройки-плейсхолдера (`PlaceholderEntityMesh`/
`PlaceholderMemoryFragmentMesh`), которые делают видимым весь бестиарий из
60 карточек без единого Blueprint — и никогда не перетирают настоящий меш
карточки, когда он появится. 261/261, два чистых прогона.

### Чистка мёртвого кода и устаревших комментариев (2026-09-02)

Скриптовый обход всех объявлений проекта (ссылки считались отдельно по
продакшену и тестам, кандидаты фильтровались по `UFUNCTION` — BP/Exec
вызовы grep не видит). Удалён целиком `GridWorldManagerHarvest.cpp`
(четыре заглушки "deprecated", две из них — видимые в консоли Exec-команды,
которые молча ничего не делали) и `BiomeGraphCommands.h` (пустая
`Register()`, никем не вызванная), а также шесть мёртвых методов и два
объявления-призрака без реализации (`SetOtherInventory`, `OnLeftClick`).
Поправлены устаревшие числа/формулировки в шапках `MemoryFragmentTypes.h`
(3 фрагмента → 12, "4 из 62 карточек бестиария" → бестиарий закрыт),
`ArtifactTypes.h`, `CommandTypes.h`. Найден и починен реальный баг:
UTF-16 мусор в `.gitignore` склеил правило `.trash/` со следующей строкой,
из-за чего оно не работало. Мёртвых полей настроек (113 шт.) и
отключённых тестов не найдено вовсе. 261/261, два чистых прогона.

### Берегиня — унификация с реестром Легендарного ранга (2026-09-02)

Ответ на "все сущности должны быть равны... без исключений" — последнее
архитектурное исключение бестиария снято, не только перенос данных.
Берегиня (per-клеточный триггер `Memory.HistoryPurity`, не якорный
MorokField, как у остальных 16) стала 17-й строкой `DT_LegendaryEntities`
через новый переключатель `bUsesCellHistoryPurity` в
`FLegendaryEntityDefinition` — принимает произвольное число будущих
сущностей того же механизма без правки C++, прямое требование "возможно
появятся подобные ей". Публичный `IsBereginyaManifested()` поглощён
`IsLegendaryManifested` (fallback-скан без якоря); Гребень получил
настоящий `LegendaryEntityID` вместо `NAME_None`. `BereginyaTest.cpp` (3
теста) прошёл без единой правки — главная регрессионная защита; один тест
в `ArtifactTest.cpp` намеренно переименован/репойнтнут (публичный метод
исчез). 261/261, два чистых прогона, подробности в CHANGELOG.md.

### Data-driven контент, юниты 4-6: Артефакты/Диалоги/Фрагменты памяти + cook-фикс (2026-09-02)

Продолжение той же миграции (пользователь: "все сущности должны быть
равны... без исключений"), тот же конвейер, что бестиарий (юниты 1-3).
`FArtifactDefinition`/`FDialogueDefinition`/`FMemoryFragmentDefinition` →
`FTableRowBase` + `SortOrder`, ленивая `LoadObject` в `GetArtifactDefinitions()`/
`GetDialogueDefinitions()`/`GetAllMemoryFragmentDefinitions()`, три новых
идемпотентных коммандлета (`ArtifactsCreate`/`DialogueCreate`/
`MemoryFragmentsCreate`) создают `DT_Artifacts` (8)/`DT_Dialogue` (1)/
`DT_MemoryFragments` (12) с нуля. Ни один продакшен вызывающий код и ни
один тестовый файл не изменился — `ArtifactTest.cpp`/`ArtifactEffectsTest.cpp`/
`DialogueTest.cpp`/`ZaryanaTest.cpp` уже читают конкретные ID карточек,
их прохождение без правок — доказательство прозрачности смены источника
данных, новых тестов не заводилось (тот же принцип, что юниты 1-3).
261/261, два чистых прогона.

Отдельно закрыт cook-инклюзия риск (замечен при ревью юнитов 1-3, не
исправлен тогда — вне скоупа): `DefaultGame.ini` получил
`+DirectoriesToAlwaysCook=(Path="/Game/Herbalist/Data")` и
`(Path="/Game/Data")` — без явной ссылки с уровня/Blueprint кукер не
находил DataTable-ассеты через граф ссылок (все грузятся строкой пути
через `LoadObject`), рискуя не включить их в паковку не-редакторского
билда.

### Data-driven бестиарий: миграция завершена, 3/3 (2026-09-02)

Последний юнит (Легендарный ранг, 16 карточек, `DT_LegendaryEntities`,
Берегиня по-прежнему вне реестра — свой per-клеточный путь) закрывает
план целиком. Итог всех трёх юнитов: 59 карточек бестиария (28 Низший +
15 Основной + 16 Легендарный) переехали из C++-литералов в DataTable —
балансировку можно тюнить без пересборки, новый тип условия/оси
по-прежнему требует кода. Ни один продакшен вызывающий код и ни один
тестовый файл не изменился ни на одном юните. 261/261 стабильно на всех
трёх, подробности каждого юнита в CHANGELOG.md.

### Data-driven бестиарий, юнит 2/3: Основной ранг (хозяева) на DataTable (2026-09-02)

Тот же рецепт, что юнит 1/3, уже проверенный на самом крупном/рискованном
случае. `FLandmarkDefinition` → DataTable (`DT_Landmarks`, 15 карточек, в
т.ч. Домовой), `LandmarksCreateCommandlet` создан по тому же паттерну.
261/261, два чистых прогона, ноль изменений в тестовых файлах.

### Data-driven бестиарий, юнит 1/3: Низший ранг на DataTable (2026-09-02)

Прямой запрос пользователя: дата-драйвен архитектура по всему контенту
проекта, не только ингредиентам. Первый из трёх юнитов (Ambient →
Landmark → Legendary): `GetAmbientEntityDefinitions()` (28 карточек)
теперь лениво грузит `DT_AmbientEntities` вместо литерального C++-массива
— `LoadObject` работает одинаково из игры/PIE/headless-теста, поэтому ни
один из ~10 продакшен вызывающих мест и ни один из 7+ существующих
тестовых файлов не изменился (261 → 261 тестов, две чистые прогона).
Новый `AmbientEntitiesCreateCommandlet` — первый в проекте коммандлет,
создающий DataTable-ассет с нуля, не только дополняющий существующий.
Пересмотрено явное прошлое возражение против DataTable в этом файле —
подтверждено пользователем. Подробности, включая находку про
GameModeBase/тесты и находки сборки, в CHANGELOG.md.

### Водные растения (2026-09-02)

Прямой запрос пользователя: растение, помеченное `bGrowsOnWater=true` на
`FIngredientTableRow`, дополнительно (не вместо обычного земляного
`AllowedBiomes`-пути) разрешено к спавну на клетках воды того же биома —
отдельный кэш `CachedAquaticResourcesByBiome` в `UIngredientRegistrySubsystem`,
новый `GetRandomResourceForAquaticBiome`. `SpawnResourcesInCell` теперь
вызывается для всех клеток (включая водные), сама выбирает пул;
возрождение после сбора расширено на воду — иначе водное растение было бы
одноразовым. Сбор воды и сбор ресурса — уже независимые механизмы, "чей
трейс попал" ничего не потребовало менять. 258→261 тест, **261/261, ноль
регрессий**.

### Вода как отдельный, явно нарисованный регион (2026-09-02)

Прямой запрос пользователя: вода — не вероятностная раскладка внутри
земляного биома, а свой сплайн-регион (`AWaterRegionVolume`, наследник
`ABiomeRegionVolume` — та же спавн-геометрия), который рисуется отдельно
и всегда побеждает (вес 1) для флага `bIsWater`, не подменяя `Cell.Biome`.
Тип воды автоматически берётся из земляного биома под ним (болото →
болотная вода, тундра → тундровая), без отдельной настройки на самом
регионе воды. Водные растения — явно оставлено "на подумать" пользователем,
не реализовано в этом заходе. 256→258 тестов, **258/258, ноль регрессий**.

### Пер-региональные настройки плотности на ABiomeRegionVolume (2026-09-02)

Прямой запрос пользователя: "настройки под все дела в этих волюмах, все
параметры, которые можно и нужно настраивать". Три числа, раньше жёстко
зашитых как одно значение на весь мир, теперь `EditAnywhere`-поля на
самом `ABiomeRegionVolume` (категория `Biome|Density`): `MinResourcesPerCell`/
`MaxResourcesPerCell`, `ResourceRegrowthTimeSeconds`, `WaterDensity`.
Новый `AGridWorldManager::GetClaimingRegion(Cell)` — какой конкретно
регион заявил клетку. Размещение воды переписано на пер-пуловый алгоритм
(регион → свой пул клеток, блок не пересекает границу пула). По дороге
найдена и починена ДО коммита реальная регрессия (другой порядок RNG даже
для случая "регионов нет" ронял `DecorativeEntitiesManifestWithoutEffect",
обнажив независимый предсуществующий баг приоритета одноранговых
существ) — случай "регионов на уровне нет" исполняет старый алгоритм
побитово, подробности в CHANGELOG.md. 253→256 тестов, **256/256, ноль
регрессий**.

### Хук для PCG-графов: саморегистрация ресурсных акторов + защита GetCell (2026-09-02)

Пользователь строит PCG Graph, спавнящий и декоративную растительность
(чистый PCG, не касается C++), и настоящие `AHerbalistResourceActor`.
Закрыт разрыв: `Cell.ResourceActors` (сбор/возрождение/сохранение/дебаг)
раньше заполнялся только C++-путями спавна — актор, поставленный PCG-графом
напрямую, не попадал бы в учёт менеджера. Новый
`AHerbalistResourceActor::RegisterOnCell()` вызывается и из `Init()`
(явная настройка из атрибутов PCG-точки), и из `BeginPlay()` (авто-
определение клетки по позиции, для актора вообще без вызова `Init()`).
Попутно найден и исправлен реальный краш (`GetCell` индексировал `Cells`
без проверки, что массив уже заполнен `InitializeCells()`) — актуален не
только для теста, но и для реального PCG-превью в редакторе до старта
игры. 251→253 теста, **253/253, ноль регрессий**.

### Клетки вне сплайнов биома больше не спавнят контент (2026-09-02)

Найдено пользователем вживую при увеличении сетки с 20×20 до 100×100 без
перерисовки сплайнов: 91% клеток попадали под блочный 5×5-фолбэк, который
раньше не только красил их каким-то биомом, но и разрешал полноценный
спавн ресурсов/сущностей — ингредиенты сыпались по всей огромной площади,
а не только внутри нарисованных `BP_BiomeVolume`. Новый
`IsCellClaimedByBiomeRegion` гейтит спавн ресурсов и биом-специфичное
проявление сущностей (Низший/Берегиня/хозяева/якоря Легендарного) клетками,
реально покрытыми хотя бы одним регионом, когда регионы вообще есть на
уровне; без регионов (тесты, сцены без PCG) — старое поведение не меняется.
"Сквозная" ночная нечисть и атмосферные суточные/сезонные нуджи не
затронуты — они не привязаны к биому по дизайну. 248→251 тест, **251/251,
ноль регрессий**. Подробности, включая разбор найденной попутно
нестабильности тестов (не связана с этой правкой), в CHANGELOG.md.

### Tier 1 точечного развития: 4 находки (2026-09-02)

Первый проход по плану "точечное развитие существующих механик" (Tier 1,
7 пунктов заявлено, 3 сняты пользователем после разведки как неприменимые
сейчас — 1.5/1.6/1.7, подробности в CHANGELOG.md). Реализовано: Ночь
усиливает ось Spirit (§15.2, был найден разрыв со старым планом — эффект
не в формуле сбора, а в TargetState-нудже), Полнолуние поднимает Морок при
варке (§15.3, `FApplyCommand::MoonPhase`), Камень-оберег теперь работает и
в ритуальной варке (известный, задокументированный побочный разрыв: заряд
после ритуала не списывается — Tier 2 п.2.4), устаревший комментарий
капищ в `MemoryFragmentTypes.h` поправлен. 246→248 тестов, **248/248, ноль
регрессий.**

### Спавн ресурсов/сущностей внутри формы PCG-биома (2026-09-02)

После подключения PCG-плагинов и расстановки трёх `BP_BiomeVolume` на
`L_TestDev` позиция спавна ресурсов/сущностей внутри клетки теперь
проверяется через `IsPointInside` формы сплайна её биома (была видна как
"дебажная сетка" — сущности стояли ровно в центре клетки, ресурсы у
границы региона могли визуально уехать в чужой биом). Клеточная модель
как расчётная единица симуляции не тронута — решение А из развилки,
явно выбранное пользователем. Новый `AGridWorldManager::
GetSpawnPositionWithinBiome`, новая настройка `EntityManifestationJitterRadius`.
Попутно найдена и починена пре-существующая гонка изоляции тестового
мира (реальные регионы с уровня утекали в `TActorIterator` каждого
теста) — `SpawnAndBeginPlay(..., KeepRegions)`, подробности только в
`CHANGELOG.md` 2026-09-02. 242→246 тестов, **246/246, ноль регрессий.**

### Артефакты/перья как настоящие предметы инвентаря (2026-09-02)

Найдена разведкой знакомства с проектом ("нет иконки/тултипа/присутствия
в инвентаре") и закрыта по прямому запросу. `ArtifactIngredientAppendCommandlet`
(-run=ArtifactIngredientAppend) добавил 12 рядов (8 артефактов + 4 пера)
в `DT_IngredientClass` (пустой `AllowedBiomes` — никогда не выпадают
случайным сбором). `AGridWorldManager::AcquiredArtifacts`/`AcquiredFeathers`
остаются единственным источником истины для эффекта — контроллер только
добавляет/убирает "квитанцию" в инвентаре (`AddArtifactToInventory`/
`RemoveArtifactFromInventory`) на 6 местах получения и 5 расходования.
Известный разрыв: ручное удаление предмета игроком не отменяет эффект.

Найдена и починена гонка `TActorIterator` в персистентном тестовом мире
(контроллер мог найти чужой, ещё не убранный сборщиком мусора
`AGridWorldManager` от предыдущего теста) — новый `SetWorldManagerForTests`
обходит её явной инъекцией, не трогая боевой `FindWorldManager()`.

237 → 242 теста, **242/242, ноль регрессий.**
