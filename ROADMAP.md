# Дорожная карта: чего не хватает и в каком порядке строить

Дата: 2026-08-23. Составлено после полной инвентаризации `Source/` и `Content/`.

Спутники: `AUDIT_AND_REFACTORING_PLAN.md` — что сломано в существующем коде;
`PROJECT HERBALIST - REFACTORING PIPELINE 2.md` — летопись сделанного;
`DESIGN_World_State.md` — модель хранения мира, разбор связности зависимостей и
проверка архитектуры (§23-26); `META_AUDIT.md` — пропуски трёх предыдущих
проходов (найдены 2026-08-23: два новых бага, мёртвый класс ингредиента,
противоречие в дизайне капищ с каноническим ГДД).

---

## 0. Диагноз

**У проекта есть сильное симуляционное ядро и почти нет игры вокруг него.**

Пайплайн, биомный граф, восприятие, детерминизм, трассировка, автотесты — всё это
живое и работает. При этом:

- **сохранений нет вовсе** — ни одного `USaveGame` во всём проекте;
- **персонаж — шаблонный `BP_FirstPersonCharacter`** из стартера UE, вместе с
  анимациями стрельбы из пистолета и `BP_JumpPad`;
- ~~**Травника (журнала) нет**~~ — ✅ ВЫПОЛНЕНО 2026-08-23, v1 (автофиксация +
  `ShowJournal`, без подсветки закономерностей — см. §2.1 ниже);
- из 544 ассетов `Content/` **свои — примерно два десятка**, остальное шаблоны
  и купленный `Stylized_Forest`;
- виджетов шесть, все — инвентарь и котёл. Нет HUD, меню, экрана мира, журнала.

Это не упрёк — нормальное состояние проекта, который рос от ядра. Но дальше
строить вглубь ядра (новые сущности, новые формулы) — значит углублять перекос.

**Главный вывод по приоритетам:** механики, спроектированные в `15_Cycles_And_Shrines`,
имеют горизонт в **игровые недели** (7 суток на зарастание клетки, 28 — лунный цикл,
117 — сезон). Без сохранений их **невозможно даже увидеть**, не то что настроить.
Persistence — не «инфраструктурная задача на потом», а предусловие для проверки
всего, что уже спроектировано.

---

## 1. Чего нет вовсе

Не заявлено в ГДД, но без этого игры не существует.

| Система | Состояние | Почему блокирует |
|---|---|---|
| ~~**Сохранение/загрузка**~~ | ✅ ВЫПОЛНЕНО 2026-08-24, v1 — см. §5 Фаза A.1 | Без неё не проверить ни один долгий процесс: стресс клетки, Restoration капищ, луну, сезоны, прогрессию. Всё спроектированное в §15 непроверяемо. |
| **Свой персонаж** | шаблон UE (`BP_FirstPersonCharacter`) | Игра про собирательство и наблюдение управляется телом шутера. Нужен свой контроллер: осмотр, приседание к траве, темп ходьбы. |
| **HUD / меню** | нет | Нет паузы, нет выхода, нет отображения времени суток — хотя часы уже тикают (`WorldTimeSeconds`). |
| **Звук** | одно поле `HarvestSound` | Игра про «читать мир по признакам» без звукового слоя теряет половину каналов обратной связи. |

---

## 2. Заявлено, центрально — и не существует

### 2.1 Травник (журнал) — ключ ко всей прогрессии

**✅ v1 ВЫПОЛНЕНО 2026-08-23.** `Core/Journal/`: `FJournalEntry` (замороженный
`PerceivedState`, не `S_real`), `UHerbalistJournalComponent` на `AHerbalistPlayerController`
(тот же паттерн, что `InventoryComponent`), автозахват в `RunSimulationStep()` по
Harvest/Apply(крафт)-командам, `ShowJournal()` как первый живой потребитель.
Подсветка закономерностей и сравнение — ещё не сделаны, это следующий шаг.

`07_UX §7.2.4` описывает его как «живого помощника»: автоматическая фиксация
каждого сбора и варки с контекстом, подсветка закономерностей, сравнение зелий.

**Почему это не просто UI.** `06_Progression` определяет прогрессию дословно как
**«сжатие ошибки между ожиданием и результатом»**. Чтобы ошибка существовала,
игроку нужно куда-то положить ожидание — и это Травник. Без него у игрока нет
инструмента накопления гипотез, а значит **прогрессии в проекте нет в принципе**,
сколько бы ни было систем.

Это делает Травник самой недооценённой позицией в списке: выглядит как «журнал
для удобства», а на деле — единственный носитель заявленной прогрессии.

### 2.2 Прогрессия — весь `06_Progression`

Раздел написан целиком и не реализован ничем. Требует: Травника (§2.1),
достроенного `S_Perceived` (§3.1) и метрики `Distance(S_real, S0)` (§2.3).

### 2.3 `Distance(S_real, S0)` — метрика состояния мира

**✅ ВЫПОЛНЕНО 2026-08-23.** `S0` пересобран (L1-нормализация Direction,
пограничные значения идеала на всех мета-осях). `HerbalistCore::Math::Distance(FRealState, FRealState)`
добавлена (11-осевая), первый потребитель — отладочная строка `GetSelectedCellInfo`
(`DistToS0`). История (`Distance_итог = Distance × (2 − AverageCoherence)`) описана
в `15_Cycles_And_Shrines §15.5.1`, ещё не реализована в коде.

### 2.4 Капища

`00_Core_Lock §5` помечает «не реализованы». Полная спецификация уже написана в
`15_Cycles_And_Shrines §15.5`: формула Restoration через Coherence варки, шесть
типов с числами, два радиуса, связь с `InventoryDecayRate` и `MorokLeak`,
осквернение как отдельное состояние. Кода нет.

Зависит от §2.3 — капище тянет клетку к `S0`.

### 2.5 Заряна: фрагменты памяти и Буян

`03_Narrative` описывает фрагменты памяти как ключи к интерпретации и Буян как
состояние мира, открывающее влияние на её судьбу. Ни того, ни другого нет.
Это единственная сюжетная награда в игре — сейчас у игрока нет ни одной причины
двигаться дальше, кроме любопытства к системе.

---

## 3. Заявлено и сделано частично

### 3.1 `S_Perceived` — сделано больше, чем я утверждал в аудите

**Поправка к `AUDIT_AND_REFACTORING_PLAN §4.1`:** я писал, что слой не реализован.
Это неточно. `FPerceptionService::PerceiveRealState`
(`Core/Simulation/Private/PerceptionService.cpp:8`) — рабочая реализация `Distort`:
шум, амплитуда которого растёт с `Distortion`, отдельно по мета-осям и по
`Direction`. `UPerceptionComponent` пересчитывает восприятие дважды в секунду,
переинициализируя `FRandomStream` одним и тем же `WorldSeed` — то есть иллюзия
**устойчива**, а не мерцает. Это хорошо сделано.

Чего действительно не хватает:

1. **Потребляет только `InventorySlotWidget`** (1 из 4). `AlchemySlotWidget` и
   `AlchemyTransferWidget` показывают `S_real` — в котле, самом ответственном месте.
2. **Нет `PerceiveClass`** — искажения самого факта «что это за вещь». Сейчас
   искажаются только числа; перепутать одну траву с похожей нельзя.
3. **Восприятие не улучшается.** Прогрессия по `06_Progression` — это сжатие
   разрыва; механизма, который бы его сжимал, не существует.

Пункт 3 — самый содержательный: он превращает восприятие из «фильтра на UI» в
собственно игру.

### 3.2 Круги времени — сутки/луна/сезон готовы v1, только погоды нет

`15_Cycles_And_Shrines` описывает три круга. Луна и сезон реализованы v1
(`ROADMAP.md` Фаза D, п.10, ниже) — только погоды нет (п.12).

**✅ Таблица суток закрыта целиком 2026-08-29** (найдено как второстепенное
2026-08-24 — механика была только у Ночи, §16.5). Теперь все четыре фазы
плюс Полудница дают эффект: Рассвет (+Purity/+Stability), Закат (+Distortion,
линейно растёт до полной силы у порога Ночи), Полудница (+Distortion,
только Степь/Лесостепь, ~2 мин в середине Дня — "получает игровые зубы"
буквально по формулировке §15.2). Регрессия: `Herbalist.DayCycle.*`
(4 теста, 45/45 всего зелёные). Подробности —
`AUDIT_AND_REFACTORING_PLAN.md §7.2`, `15_Cycles_And_Shrines.md §15.2`.

### 3.3 Бестиарий — 4 из 62

И у трёх из четырёх реализация разошлась с §16 (см. аудит §4.5), а Морочники
не видны игроку вовсе.

---

## 4. Инфраструктурный долг

| Позиция | Почему важно |
|---|---|
| **`DA_BiomeGraph` → JSON** | Блокер баланса. `MorokAffinity` определяет, срабатывает ли Bifurcation вообще, лежит в бинарнике, не ревьюится, не выводится из лора. Паттерн готов — `extract_biomes.py`. |
| **Дублирование презентационной логики в виджетах** | *(Уточнено 2026-08-23, см. `DESIGN_World_State.md` §23 — было описано неточно как «нет ViewModel».)* Путь записи в порядке: всё реально идёт через `QueueCommand`, напрямую или тонкими обёртками. Проблема — в чтении: каждый виджет сам разрешает имя предмета и perceived-состояние, отсюда и разъехались «Ash» и «Зола». |
| **`FCommandGraph` — не граф** | *(Уточнено 2026-08-23.)* Command Algebra Q/T/D/S/B уже реализована как размеченный union (`FCommandEntry`), не плоский enum. Но `FCommandGraph` — плоский массив с `ExecutionOrder`, без рёбер зависимости; имя обещает больше, чем есть. `Talk` (B) — заглушка. |
| **PCG-визуализация** | Задел уже есть: `MPC_WorldStateFields.uasset` — Material Parameter Collection, кто-то начал выводить состояние мира в материалы. Ниточка не оборвана. |
| **Мусор в `Content/`** | `NewBlueprint.uasset`, `NewMaterial.uasset`, шутерный шаблон целиком (анимации пистолета, `BP_JumpPad`, `BP_WobbleTarget`). |

---

## 5. Дорожная карта

Порядок продиктован зависимостями, а не привлекательностью задач.

### Фаза A — «игру можно проверить»

Без этого нельзя оценить ничего из уже написанного.

1. ✅ **Сохранение/загрузка** — ВЫПОЛНЕНО 2026-08-24, v1 (`Core/Save/`).
   Не полный Шаг 5 миграции хранения (`DESIGN_World_State.md` §3: сид + источники
   влияния — источников как отдельного слоя ещё нет, Шаги 2-4 не начаты), но тот
   же принцип «сохраняем только тронутое»: `AGridWorldManager::DirtyCellIndices` —
   липкое множество клеток, помечаемых в каждой реальной точке записи
   (`ApplyStateDelta`, релаксация, сбор/отрастание ресурса), `CaptureSaveCells()`
   обходит только его, а не всю сетку. `RngBaseSeed` сохраняется, но
   `WorldRNG`/таймер зарастания клетки не продолжаются бит-в-бит — принятое
   упрощение v1, см. `UHerbalistSaveTypes.h`.
   Игровые часы (`GameClockSeconds`, не движковое `GetWorld()->GetTimeSeconds()`)
   тоже сохраняются отдельно — фаза суток и будущая погода через
   UltraDynamicSky (Фаза D §12) должны возобновляться с той же точки, а не с
   рассвета каждой сессии; правка сделана по прямому требованию не резать это
   как одноразовое упрощение.
   Заодно найден и починен независимый баг: `FBiomeDefaults::BiomeDataTable` —
   сырой `static`-указатель без GC-ссылки, собирался мусорщиком через ~60 сек
   в `-game` сессии (`AddToRoot`/`RemoveFromRoot`).
   Регрессия: `Herbalist.Save.CaptureIsSparse`, `Herbalist.Save.ApplyRestoresEarlierSnapshot` —
   первые автотесты проекта, реально спавнящие `AGridWorldManager` в мир
   (`DispatchBeginPlay()`), а не только чистые функции/подсистемы без мира.
2. ✅ **`DA_BiomeGraph` → JSON** (§4) — ВЫПОЛНЕНО 2026-08-24, экспорт-направление.
   `UBiomeGraphExportCommandlet` (`ProjectHerbalistTests`, `-run=BiomeGraphExport`)
   читает живой asset через `LoadObject` (у `UDataAsset`, в отличие от
   `UDataTable`, нет штатного Import/Export) и пишет `herbalist_docs/CSV_tabs/DA_BiomeGraph.json`.
   Импорт (JSON -> запись пакета обратно) сознательно не сделан — экспорт закрывает
   названную боль «не ревьюится», запись пакета через `UPackage::SavePackage`
   рискованнее, отдельная задача при необходимости.

   **Первый же экспорт вскрыл именно то, чего боялся ROADMAP:** у всех 8 узлов
   `MorokAffinity = ZaryanaAffinity = Stability = 0.5` — это дефолты C++-структуры,
   их НИКТО никогда не задавал осознанно. Bifurcation сейчас биом-слеп: болото
   рискует испортить варку ровно так же, как тундра, вопреки всему лору и
   `DT_BiomeDefaults` (Corruption Болота 0.70 против, например, 0.10-0.20 у леса).
   Плюс всего 3 рёбра из возможных: Tundra-Taiga-MixedForest и Steppe-Floodplain
   связаны, **Болото не соединено вообще ни с одним биомом** — Морок/Заряна не
   могут ни утечь из него, ни в него прийти.

   **✅ ДОДЕЛАНО 2026-08-24** (значения были тестовыми — «они всё равно тестово
   там стоят»). Добавлен `UBiomeGraphImportCommandlet` (`-run=BiomeGraphImport`,
   пишет JSON обратно в живой asset через `UPackage::SavePackage` — риск
   отдельно от чтения, поэтому отдельный коммандлет, не флаг того же).
   `MorokAffinity`/`ZaryanaAffinity`/`Stability` выведены из уже проверенных
   `Corruption`/`Purity`/`Meta.Stability` в `DT_BiomeDefaults.json` (тот же
   принцип, что дал `StressRecoveryMultiplier`) — Болото 0.70/0.35/0.35, Тайга
   0.18/0.80/0.70. Граф — связное дерево на все 8 узлов по стандартной
   зонации (Тундра-Тайга-Смешанный-Широколиств.-Лесостепь-Степь), Болото
   подключено через Пойму; каждая связь теперь в обе стороны (14 рёбер) — было
   по одной, диффузия физически не могла течь назад. Записано в asset и
   проверено обратным экспортом (round-trip совпал).
3. ✅ **Достроить `S_Perceived` до котла** — ВЫПОЛНЕНО 2026-08-24.
   `AlchemySlotWidget` теперь считает `PerceivedState` (тот же `PerceiveRealState`,
   что и везде) один раз при входе предмета в слот — как имя зелья, так и
   искажённые числа, а не при каждой перерисовке (не мигает). `AlchemyTransferWidget`
   раньше сливал `Item.State.Magnitude`/`.Meta.Distortion` игроку в статусной
   строке буквально дословно — самый прямой утечка `S_real` изо всех виджетов;
   теперь читает уже посчитанный `ResultSlot->GetPerceivedState()`, а не считает
   заново своим RNG (иначе цифры в слоте и в тосте не совпадали бы).
   Морочники в UI (крит §1.3 аудита) — уже было закрыто раньше сессии
   (`ItemTooltipWidget::SetItem`), отдельной правки не потребовалось.
   Не проверено интерактивно (PIE) — см. `feedback` в чате сессии, headless
   UI-тестирование виджетов в этом проекте пока не настроено.

После фазы A впервые можно сесть и играть достаточно долго, чтобы увидеть
зарастание клетки и работу Морока.

### Фаза B — «игра учит»  ✅ ВЫПОЛНЕНА ЦЕЛИКОМ 2026-08-23

4. ✅ **`S0`** починен (аудит §2.3): L1-нормализация, пограничные значения идеала
   на шести мета-осях.
5. ✅ **`Distance(S_real, S0)`** реализована как метрика (`HerbalistCoreMath.h`);
   потребитель пока один (отладочная строка) — капища (Фаза C, §7) станут вторым.
6. ✅ **Травник** v1: автофиксация сбора и варки с контекстом. Подсветка
   закономерностей — следующий шаг, не блокирует остальную Фазу C.

Заодно, вне исходного плана Фазы B, закрыто **звено 3** связности зависимостей
(`DESIGN_World_State.md §8/§15`): выбор ресурса при спавне теперь зависит от
`Cell.State`, не только от enum'а биома — испорченная клетка рожает другой набор
трав, чем чистая. Это было независимо готово к реализации (пригодность через уже
существующий `BaseState`/`Distance`) и не требовало ждать капищ.

### Фаза C — «у игры есть дуга»

7. ✅ **Капища** — ВЫПОЛНЕНО 2026-08-24, v1 (эффекты 1/2/4 из четырёх, без типов
   и Буяна). Подробности и найденный по пути пререквизит (клетка котла) —
   `15_Cycles_And_Shrines.md §15.6`. Регрессия: `Herbalist.Shrine.*` (4 теста).
8. ✅ **Улучшение восприятия** — ВЫПОЛНЕНО 2026-08-24, с уточнением по ходу
   сессии: `06_Progression.md` прямо запрещает «уровни, числовые характеристики
   и прямые улучшения» — значит это не правка формулы шума `PerceiveRealState`
   (это и было бы скрытым числовым баффом), а инструмент **сравнения** уже
   собранных записей Травника: игрок сам решает, что взять за эталон, глядя
   на несколько искажённых показаний одного ингредиента рядом («это как
   селекция», формулировка из сессии). `UI/JournalWidget` + `UI/JournalEntryRowWidget` —
   плоский список записей (новые сверху), ничего не усредняет и не подсказывает.
   `UHerbalistJournalComponent::GetEntriesForIngredient` (был объявлен ещё при
   сборке Травника v1, ни разу не вызван) — теперь тоже есть где использовать,
   хоть в v1 экрана фильтрация по ингредиенту ещё не подключена к UI.
   **Не хватает для игры:** WBP-блюпринты для `JournalWidget`/`JournalEntryRowWidget`
   и Input Action asset для открытия экрана — оба нужно создать в редакторе
   (тот же паттерн, что уже у `InventoryWidgetClass`/`InventoryAction`, C++ этого
   не может). До этого открыть экран можно только через `ToggleJournalUI()`
   (Exec) — и то бесполезно без WBP.
9. ✅ **Фрагменты памяти Заряны** + условие Буяна — ВЫПОЛНЕНО 2026-08-24.
   Подробности — `DESIGN_World_State.md` Часть IX, `15_Cycles_And_Shrines.md §15.5`.
   Регрессия: `Herbalist.Zaryana.*` (3 теста).

**Фаза C закрыта целиком** (7/7/8/9 — все пункты выполнены 2026-08-24).

### Фаза D — «мир живёт»

10. Луна и сезоны (`15_Cycles §15.3-15.4`).
    ✅ **Луна, v1, ВЫПОЛНЕНО 2026-08-24** — `AGridWorldManager::GetMoonPhase()`
    (4 фазы по `StressRecoveryGameDays` игровых суток каждая, из
    `GameClockSeconds`), эффект пока только на сбор: Растущая усиливает
    Body/Nature/Magnitude, Полнолуние — Spirit/Potency/Resonance
    (`GenerateHarvestResult`, `PipelineV2.cpp`). Эффекты Новолуния/Убывающей
    (усиливают применённое зелье, не сбор) и рост Morok вместе с Полнолунием
    — не в v1, требуют захода в формулу варки. Регрессия:
    `Herbalist.MoonPhase.*` (4 теста, 37/37 всего зелёные).

    ✅ **Сезоны, v1, ВЫПОЛНЕНО 2026-08-24 (тот же день)** —
    `AGridWorldManager::GetSeason()`, длина сезона — новая настройка
    `SeasonDurationDays` (117 суток). Весна/Зима модулируют срок зарастания
    клетки (`StressRecoveryMultiplier`, поверх биомного), Зима вдобавок
    поднимает Purity по всей сетке ("снег как чистота", тот же паттерн, что
    ночной нудж §16.5). Лето — намеренно нейтральный сезон, спецификация не
    даёт для него числа. Fertility (заявлено в §15.4) НЕ реализована — поле
    `FEnvironment::Fertility` мёртвое, нигде не читается; зимнее окно для
    фрагментов Заряны — отдельная задача, не в этом заходе.
    **Реальная находка:** первая версия перепутала знак
    `StressRecoveryMultiplier` (это множитель времени восстановления, не
    скорости — Весна должна быть <1.0, не >1.0), поймано собственным
    автотестом `Herbalist.Season.SpringSpeedsUpStressRecoveryWinterSlowsIt`
    до коммита, не постфактум. Регрессия: `Herbalist.Season.*` (3 теста,
    40/40 всего зелёные).
11. Бестиарий пачками по механизмам — сначала привести 4 существующих в
    соответствие с §16, потом расширять.
    ✅ **Первая пачка ВЫПОЛНЕНА 2026-08-24:** §16.2 (Низший) переведён с
    захардкоженного if-блока (только Гнильники) на таблицу данных
    (`Core/Entities/AmbientEntityTypes.h` + обобщённый цикл в
    `UpdateEntityManifestations`) — именно то, что сама спецификация §16.2
    называет "новый код не нужен, только данные". Добавлены Моховые духи
    (Тайга, единственный "улучшающий" Низший) и Степные огни (Степь, ночной
    триггер) — 3 из ~30 карточек §16.2. Остальные ждут луны/сезонов/погоды
    (10, 12) или другого механизма (§16.3 "подношение", отдельная система
    порчи инструмента). Регрессия: `Herbalist.AmbientEntity.*` (3 теста,
    включая ночной §16.5, см. ниже).

    ✅ **Вторая пачка ВЫПОЛНЕНА 2026-08-24 (тот же день, продолжение):**
    Берегиня (§16.4) реконсилирована — Restoration капища поблизости теперь
    второй независимый путь к триггеру, не только `HistoryPurity` (регрессия:
    `Herbalist.Bereginya.*`, 3 теста, первые на Берегиню вообще). §16.5
    "сквозная ночная фаза" получила первую механику: разлитый по всей сетке
    ночной нудж Distortion/Corruption, не претендующий на `ManifestedEntityID`
    (эта категория бестиария не "владеет" клетками, она разлита по карте).
    Полевик (§16.3) сознательно НЕ приведён к спецификации — формула §16.3
    завязана на Coherence варки, что противоречит более позднему решению той
    же сессии ("варка не влияет на клетки мира"); расхождение осознанное.
    33/33 автотестов зелёные.
12. Погода — через готовый **UltraDynamicSky** (решено 2026-08-23), то есть
    задача не «сделать погоду», а связать её параметры с пригодностью
    ингредиентов и проявлением сущностей (см. `DESIGN_World_State.md` §8).
13. PCG/материалы — продолжить с `MPC_WorldStateFields`.

### Вне фаз, в любой момент

- Свой персонаж вместо шаблона, HUD, звук.
- Чистка `Content/` от шутерного шаблона.
- ~~Перенос стресс-скриптов (`stress_ingredients.py`) в `ProjectHerbalistTests`~~ —
  сами скрипты не сохранились (упомянуты в `AUDIT_AND_REFACTORING_PLAN.md §6`
  как «скрипты сохранены», но `find` по репозиторию 2026-08-24 их не нашёл).
  Вместо переноса — написана новая регрессия `Herbalist.Resilience.*`
  (3 теста) прямо на живом пайплайне (`ExecutePipeline`, не Python-порт),
  закрывающая ровно ту границу формулы (`IngredientTableRow::Resilience`),
  которую стресс-скрипты были призваны проверять.
- ✅ **Аудит 2026-08-24 (внеплановый, в рамках "продолжай в авторежиме"):**
  `AlchemySlotWidget` не читал `GlobalPerceptionClarity` (единственный
  потребитель perceived-состояния мимо неё); `TrySpawnStateBasedFragment`
  всегда выбирал первую подходящую клетку по обходу, а не случайную;
  `Delta.TargetStateNudges` в `GridWorldManagerEntities.cpp` — два цикла
  (Гнильники/Берегиня и Полевик) могли молча затереть нудж друг друга при
  коллизии клеток (`AUDIT_AND_REFACTORING_PLAN.md §3.6`, было заложенной
  миной, не багом сегодня — починено превентивно).
  Заодно найдено и закрыто: `IngredientTableRow::Resilience` не имела ни
  одного осознанно заданного значения ни у одного из 71 ингредиента —
  добавлены 5 диагностических/фольклорных карточек (Дубовая кора, Дождевик,
  Плакун-трава, Белена, Аконит — каждая целится в конкретную границу
  формулы сбора, звено 3) через новый `IngredientAppendCommandlet`
  (`-run=IngredientAppend`, тот же принцип, что `BiomeGraphImportCommandlet`,
  но через штатный `UDataTable::GetTableAsJSON`/`CreateTableFromJSONString`,
  а не ручную сборку JSON — существующие 72 ряда не пересобирались вручную).
  Побочно найден и починен реальный краш: UBT's adaptive-unity build
  (`git status --porcelain`-парсинг) падал с необработанным исключением
  на кириллических именах файлов — теперь всегда, раз в репозитории есть
  кириллические `.md` (не только сегодняшние 5 новых) — отключено через
  `SourceFileWorkingSet.Provider = None` в `BuildConfiguration.xml`
  (не git config — влияет только на то, какие файлы исключаются из unity
  при инкрементальной сборке, на корректность программы не влияет).
  Регрессия: `Herbalist.Resilience.*` (3 новых теста, 26/26 всего зелёные).

---

## 6. Что я бы сделал первым, если выбирать одну задачу

**✅ Находка §7.1 подтверждена и починена 2026-08-24, доведена до порядка
2026-08-29.** Автотест `Herbalist.Save.BiomeInfluencesWithZeroFieldsStaySparse`
живьём воспроизвёл баг (400 из 400 клеток грязнились от нулевых полей) — не
только чтением кода. `ApplyBiomeInfluences` (`GridWorldManagerCore.cpp`)
правлен: `if (MorokField)` (есть ли запись в TMap) заменено на сравнение
"изменилось ли значение на самом деле" через `FMath::IsNearlyEqual`. По
просьбе пользователя ("наведём порядок в архитектуре") то же правило
доведено до ночного/зимнего нуджа (§16.5/§15.4, `GridWorldManagerEntities.cpp`) —
раньше оба безусловно писали `Delta.TargetStateNudges` даже у клампленного
потолка/пола без реального сдвига. 41/41 всего тестов зелёные.

**Это тактически достаточное, а не временное решение** — не первый шаг
перед "настоящим" фиксом. Полный архитектурный переход (Morok/Zaryana как
read-time оверлей, не персистентная запись в клетку — вариант 2
`AUDIT_AND_REFACTORING_PLAN.md §7.1`) осознанно отложен: требует ЗАВЕСТИ
сохранение для `BiomeGraphSubsystem::Nodes` (сейчас нигде не сохраняется —
проверено при повторном чтении перед этой правкой) и трогает общий цикл
релаксации, которым пользуются бистабильность/капища/Гнильники/Берегиня —
непропорциональный blast radius сверх того, что уже дал тактический фикс.
Подробности — `AUDIT_AND_REFACTORING_PLAN.md §7.1/§7.3`.

**Дальше по важности:**

**`DA_BiomeGraph` в JSON** — экспорт/импорт уже есть (`ROADMAP.md` Фаза A,
п.2), но сам граф с сессии 2026-08-23 не пересматривался под свежие
находки — не блокирует, но стоит держать в поле зрения при следующей правке
баланса Морока.

**Тестовая дыра, тот же корень, что §7.1** — ни один текущий автотест не
гоняет настоящий `Tick()` с `BiomeGraphSubsystem` активным; интеграционный
тест "потикать N секунд, проверить `DirtyCellIndices.Num()` не улетает до
размера сетки" закрыл бы и находку §7.1, и защитил бы от такого же класса
регрессии в будущем — тот самый "среда тестирования", которую просил завести
изначальный запрос на автономную работу.
